{
  "steps": [
    { "op": "initialize" },
    { "op": "newSession" },
    { "op": "prompt", "text": "Read request event 5 with session_inspect, verify the complete spill was retained, then reply DONE." }
  ]
}
