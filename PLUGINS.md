# Harniverse Plugin Inventory

This document records the exact DeepSeek Harness plugin baseline imported by Harniverse and the downstream plugin changes made on top of it. It distinguishes npm package inventory, Cordis plugin roles, capability seams, and shipped composition changes; package count alone does not describe the architecture.

## Comparison Baseline

| Item | Value |
|---|---|
| DeepSeek Harness commit | `47f943859bef60e4160492346772ded9b24f765a` |
| Harniverse import commit | `440d2ae5a426613bd14c4c601a90f79e4b01c149` |
| Shared tree | `f904efab9ef435201d6ba4da88a34d6366568272` |
| Current Harniverse commit at document creation | `795f93ba8409686add70978d86b56a4e9b6caae0` |
| Official `packages/*/*` package count | 219 |

The two baseline commits have the same Git tree. All downstream statements therefore describe `440d2ae5a4..HEAD` without upstream-version ambiguity. The official inventory below covers first-party packages under `packages/*/*`; vendored Cordis packages are framework dependencies, while `apps/*` are launchers and assembled application hosts.

Workspace workbench implementation tracking: `9a74618b20`; real-browser interaction hardening: `f986355714`; navigation, preview, and glob integration: `6fdf45691a`; session-control work-status response baseline: `fd4a04a42cc08ca07ccb09143ffdc2870b3cad5f`; native macOS shell and Intel runtime coverage: `97a196e925`; native macOS process-path portability: `bcc4186fb5`; native macOS resource-bound portability: `71befd15fe`; native Windows lease-cleanup contention: `277be8ca8a`; global compaction controls: `5a794a57bd`; client-aborted HTTP request handling: `2b8ee0cc0f`; workbench session-activity revalidation: `9c55cda6ba6424ac5d60d0383b66852b971c4453`.

Session checkpoint recovery implementation tracking: `0b617df8d6fd04d0f73dba3afdd4d969cfe07972`.

Exact-config HMR readiness implementation tracking: `7b3ab6ae97595affd9ca12e3bd9ca3a987a98f7e`.

Enrollment invitation implementation tracking: `b33190a76f3a7b6f451dd19d2a1d98b03e57e04f`.

## Official Plugin Inventory

<!-- official-inventory-start -->
The inventory is grouped by package directory. Every listed package is an official first-party workspace package at the baseline tree; a package may expose a Service Definition, Service Provider, Consumer, bundle, UI plugin, support plugin, or more than one tightly coupled role.

| Group | Count | Packages |
|---|---:|---|
| `acp` | 1 | `acp` |
| `api` | 2 | `gateway`, `remotes` |
| `attachment` | 2 | `attachment`, `attachment-local` |
| `boot` | 3 | `app-boot`, `cmdline`, `hmr-coordination` |
| `bundle` | 3 | `base`, `headless`, `web-app` |
| `client` | 39 | `connection`, `hmr`, `locale`, `modules`, `runtime`, `schema-form`, `ui-agent-preset`, `ui-attachment`, `ui-commands`, `ui-conversation`, `ui-deliverables`, `ui-directory-picker-browse`, `ui-directory-picker-native`, `ui-goal`, `ui-input-trigger`, `ui-jobs`, `ui-layout`, `ui-message-feedback`, `ui-model-selection`, `ui-permission-presets`, `ui-plan`, `ui-primitives`, `ui-settings`, `ui-settings-general`, `ui-settings-models`, `ui-settings-plugin-inventory`, `ui-settings-plugins`, `ui-sidebar`, `ui-skill`, `ui-slots`, `ui-subagent`, `ui-theme`, `ui-tool`, `ui-trajectory`, `ui-user-questions`, `ui-workflow-run`, `ui-workspace`, `web`, `web-react` |
| `code-runtime` | 2 | `code-runtime`, `code-runtime-worker-thread` |
| `compaction` | 4 | `command-compact`, `compaction`, `compaction-basic`, `compaction-tool-result-pruner` |
| `context` | 6 | `agent-instructions`, `command-reset`, `context-reset`, `session-reference`, `time-context`, `tmux-context` |
| `core` | 10 | `agent`, `agent-default-model`, `agent-loop`, `agent-tool-presentation`, `model-policy`, `model-policy-fallback`, `scope`, `session`, `system-prompt`, `tools` |
| `credentials` | 2 | `credentials`, `credentials-local` |
| `examples` | 3 | `acp-demo`, `agent-spine-demo`, `jsonrpc-demo` |
| `extensions` | 4 | `cordis-client-runner`, `cordis-host-runner`, `tool-cordis`, `ui-cordis` |
| `feedback` | 2 | `command-feedback`, `message-feedback` |
| `fs` | 7 | `fs`, `fs-local`, `fs-observation-policy`, `fs-sandbox`, `tool-fs`, `tool-fs-search`, `tool-str-replace-editor` |
| `goal` | 4 | `command-goal`, `goal`, `goal-round-driver`, `tool-goal` |
| `guard` | 2 | `repeat-tool-reminder`, `timeout-policy` |
| `hooks` | 3 | `hook-protocol`, `hooks-claude-code`, `hooks-codex` |
| `host` | 8 | `apiproxy`, `directory-picker`, `directory-picker-auto`, `directory-picker-browse`, `directory-picker-native`, `frontend-static`, `plugin-inventory`, `webserver` |
| `identity` | 1 | `anonymous-user-id` |
| `interaction` | 5 | `commands`, `permission-presets`, `tool-ask-user`, `user-approval`, `user-questions` |
| `jobs` | 3 | `jobs`, `jobs-local`, `tool-jobs` |
| `llm` | 5 | `llm`, `llm-deepseek`, `llm-pi-ai`, `llm-retry`, `token-meter` |
| `lsp` | 3 | `lsp`, `lsp-stdio`, `tool-lsp` |
| `mcp` | 1 | `mcp-client` |
| `plan` | 1 | `plan-mode` |
| `preset` | 2 | `agent-presets`, `persona` |
| `runtime-diagnostics` | 1 | `invariants` |
| `sandbox` | 4 | `sandbox`, `sandbox-local`, `sandbox-policy`, `sandbox-windows-acl` |
| `schedule` | 2 | `scheduler`, `tool-scheduler` |
| `sdk` | 3 | `client`, `protocol`, `server` |
| `session-query` | 4 | `session-log-export`, `session-query`, `session-query-sqlite`, `tool-session-query` |
| `session` | 13 | `session-checkpoint-policy`, `session-persistence`, `session-persistence-jsonl`, `session-persistence-sqlite`, `session-projection`, `session-projection-cache`, `session-stats`, `session-telemetry`, `session-telemetry-otel`, `session-title`, `session-title-all-prompts-llm`, `session-title-first-prompt-llm`, `session-title-llm` |
| `settings` | 2 | `settings`, `settings-file` |
| `shell` | 9 | `bash-local`, `bash-sandbox`, `pwsh-local`, `pwsh-sandbox`, `shell`, `shell-env`, `tool-bash`, `tool-bash-persistent`, `tool-pwsh` |
| `skill` | 4 | `skill`, `skill-badge`, `skill-filesystem`, `tool-skill` |
| `spill` | 3 | `spill`, `spill-local`, `spill-policy` |
| `storage` | 4 | `storage`, `storage-domain`, `storage-json`, `storage-sqlite` |
| `subagent` | 11 | `subagent`, `subagent-acp`, `subagent-claude-code`, `subagent-codex`, `subagent-dsh-sdk`, `subagent-fork-in-process`, `subagent-in-process-driver`, `subagent-spawn-in-process`, `tool-subagent`, `tool-subagent-control`, `tool-subagent-report` |
| `subprocess` | 2 | `subprocess`, `subprocess-local` |
| `terminal` | 3 | `terminal`, `terminal-bash`, `tool-terminal` |
| `test-support` | 7 | `acp-snapshot`, `agent-loop-testkit`, `client-runtime`, `llm-mock-server`, `llm-replay`, `loader-smoke`, `remote-mock` |
| `todo` | 1 | `tool-todo` |
| `typert` | 4 | `generator`, `loader`, `protocol`, `registry` |
| `util` | 8 | `atomic-write`, `brand`, `home-paths`, `lazy-require`, `launch-environment`, `native-command`, `output-retention`, `timeout` |
| `web` | 6 | `tool-web`, `web`, `web-fetch-http`, `web-search-deepseek`, `web-search-exa`, `web-search-perplexity` |
| `workflow` | 4 | `tool-ralph`, `tool-workflow`, `workflow`, `workflow-worker-thread` |
| `workspace` | 1 | `workspace` |
<!-- official-inventory-end -->

## Harniverse Capability Changes

<!-- capability-changes-start -->
Harniverse groups downstream package manifests by complete capability family rather than presenting each package as an independent capability.

| Capability | Service Definition | Service Provider | Consumer | Current downstream status |
|---|---|---|---|---|
| Per-session supervision mode | `dsh-supervision` (new; durable mode owner and model-context Consumer) | `dsh-supervision` | `dsh-user-questions`, `dsh-user-approval`, `dsh-plan-mode`, `dsh-agent-presets`, `dsh-subagent`, `dsh-client-ui-conversation` | Complete independent session policy; `supervised` preserves human questions, approval, and plan review, while `unsupervised` fails new human-dependent requests before provider/pending state, prevents non-plan entry, and allows existing plans to exit autonomously. Profile and Child Profile selection pins the mode in durable Session events at publication/delegation boundaries. |
| Session Model Profiles and Routes | `dsh-model-policy` (new; settings, durable Profile snapshots, and target authorization) | `dsh-model-policy`, `dsh-model-policy-fallback` (new; ordered cross-model recovery) | `dsh-host-apiproxy`, `dsh-client-ui-model-selection`, `dsh-client-ui-settings-models`, `dsh-compaction-basic`, `dsh-session-title-llm` | Implemented in the current worktree; Profile snapshots preserve existing Session permissions, model/Route/Profile API mutations are server-authorized, and fallback transitions are durable. Tracking SHA is pending the explicitly deferred local commit. |

| Inbound authentication and authorization | `dsh-authentication` (new) | `dsh-authentication-local` (new) | `dsh-client-connection` (official, modified), `dsh-auth-app` (new management bundle), `dsh-sdk-client` (official, modified) | Complete seam; public-key Grants produce short credentials bounded by Grant deadlines, browser sessions renew through device possession at half-life and on lifecycle recovery, human-readable enrollment is bounded, loss of the final owner seals every business transport, declared Host/Origin trust precedes authentication, endpoint metadata defaults unknown operations to deny, and concurrent admissions share durable registry and audit I/O without merging per-request decisions or records. CLI-issued one-time enrollment invitations (`dshi1_` tokens, hashed at rest in `grants.json`) redeem as pre-issued approvals with capability ceilings, uniform 401 replay semantics, invalid-only limiter counting, and same-key pending replacement; the gate redeems from the form and pending screens with a retained-key recovery path. |
| Browser authentication lifecycle | `dsh-client-authentication` (`BrowserAuthenticationService`) | Same package, adopting the single lightweight-bootstrap runtime | `dsh-client-web`, `dsh-client-connection`, `dsh-client-hmr`, module-load admission, and `dsh-client-ui-connection-status` | Shared proactive renewal and classified pre-dispatch 401 recovery; per-caller cancellation, terminal refresh instruction without automatic navigation, and a read-only sidebar status slot. The Host credential lifetime and authorization rules are unchanged. |
| Outbound notification | `dsh-notification` (new; Definition + coordinator Consumer) | `dsh-notification-http` (new) | Coordinator folded into `dsh-notification` | Complete opt-in seam following the official session-telemetry pattern. |
| Durable result artifacts | `dsh-spill` (official, modified) | `dsh-spill-local` (official, modified) | `dsh-tool-result-artifacts` (new), `dsh-spill-policy` | Complete seam; one Consumer owns retention, recovery marker, failure semantics, and `artifact_read`. |
| Recallable and model-directed compaction | `dsh-compaction` (official, modified) plus `dsh-compaction-lossless` summary-DAG service (new) | `dsh-compaction-lossless` (new; inherits the official basic Provider transaction) | `dsh-command-compact` (official, modified), `dsh-tool-compaction` (new), `dsh-tool-compaction-history` (new) | Complete shipped seam; automatic, human-requested, and direct model-requested compaction share the Provider transaction and committed checkpoints over the canonical Session log. The root-only `dsh-compaction-settings` plugin owns one live global pressure-threshold override while exact model policy and Profile-scoped Provider ownership remain intact; bounded current-session tools search summaries and expand parent/source history. |
| Read-only plugin diagnostics | `dsh-plugin-diagnostics` (new) | `dsh-plugin-diagnostics-cordis` (new) | `dsh-host-plugin-inventory`, `dsh-api-remotes`, `dsh-client-ui-settings-plugin-inventory` (official, modified) | Complete shipped advisory seam; effect-scoped checks observe Host Loader, standing preset, and dynamic Cordis lifecycle state, while the authorized Remote and existing Settings tab expose structured findings and textual hints without a repair operation. |
| Read-only workspace inspection | `dsh-api-remotes` and `dsh-workspace` (modified) | `dsh-host-apiproxy` (modified) | `dsh-client-connection`, `dsh-client-runtime`, `dsh-client-ui-layout`, `dsh-client-ui-slots`, `dsh-client-ui-workspace` (modified) | Complete read-only seam; authenticated bounded file, search, binary-preview, and Git RPCs feed a root-scoped per-Workspace Workbench whose browser-memory content state shares one mutually exclusive right region with Session details. Files, Changes, and Search are top-level peer tabs with Search third, and contributed sections (browser, terminal) register tabs and bodies beside them through the workbench-section holes with the selected section riding the layout store (`openWorkbenchSection` routes straight to one); the workbench serves blank sessions too through an input-dock chip while their hidden session header keeps the capsule button off; include/exclude glob filters compile once at the Host boundary and travel through the typed Client/runtime wire. The non-modal desktop preview occupies only the conversation track through `shell.overlay`, while the responsive modal drawer switches the same preview surface into the whole right panel. POSIX file and Git reads bind to validated descriptors, Git rejects work-tree or metadata escapes, and both placements preserve keyboard and accessibility boundaries. |
| LLM wire-attempt diagnostics | `dsh-llm` and `dsh-session` (modified) | `dsh-llm-deepseek`, `dsh-llm-pi-ai` (modified) | `dsh-agent-loop` (modified) | Complete session-indexed transport record; each adapter attempt records compact protocol/request fingerprints, status, diagnostic headers, timing, and normalized failures while the append-only Session log remains the sole owner of conversation content. |
| Agent Profile plugin composition | `dsh-capabilities` (new; recipe/member catalog, typed override store, conflict-checked planner) | `dsh-agent-presets` (official, modified; static recipe/config compiler and generation runtime), `dsh-host-capability-management` (new; authorized Remote) | `dsh-tools`, `dsh-skill`, `dsh-mcp-client`, `dsh-mcp-user-config` (new), `dsh-client-ui-settings-capabilities` (new) | Complete composition seam for Tool, Skill, MCP-server, and Subagent-provider recipes; catalog reads mount no Profile, global/Profile selection, immutable-member allowlist, and owner-declared configuration inheritance drive native Loader patches and registry restrictions, hard dependencies auto-load, running Sessions stay pinned, and each live Session exposes its immutable generation and resolved members. Standard, the `code` preset, and Cordis load user MCP consumers by default while Minimal omits them. Global defaults do not union Profile-native rows, PTC is opt-in, and `run_code` is exposed as a selectable member. Two selected recipes claiming one member name are refused as a plan blocker, and an already-stored conflict compiles with the opted-in row owning the name so the generation still mounts. |
| User Hook compatibility | `dsh-hook-protocol` (official, modified) | `dsh-hooks-claude-code`, `dsh-hooks-codex` (official, modified) | Standard, Code, and Cordis Profile compositions | Complete generic discovery path; dialect bridges remain available for explicit opt-in, but are disabled in every shipped composition and remain absent from Minimal by default. When enabled, they load per-event immutable snapshots from user/project/plugin/policy sources and honor disabled entries. |
| Parent-private Child Profiles | `dsh-subagent` (official, modified; resolved profile contract, parent-private registry, durable descriptor snapshot, fallback routes, priority gate, and unified sync/async Invocation entry) | `dsh-subagent-in-process-driver`, `dsh-subagent-dsh-sdk` (official, modified; native/SDK workspace, route, Tool, Skill/MCP setup) | `dsh-tool-subagent` and `dsh-tool-subagent-control` (official, modified; optional define/list, `child_profile_id` delegation, and bounded authorized child-history reads) | Active seam: Host-resolved profiles carry only opaque harness/model-route/capability references, reject requests outside the parent grant, enforce descendant workspace paths and profile-bounded Tool filters, resolve primary routes with ordered failure fallback, gate lower-priority child steps behind active higher-priority children, persist immutable revision/digest snapshots, and cross the SDK initialize handshake. The service returns one Invocation vocabulary for synchronous result waits and asynchronous durable child turns; native results render a child Session id, inspection accepts both modes, and later-turn delivery accepts continuable asynchronous children. Ordinary Host list/search/increment surfaces and the all-session mux baseline exclude child Sessions; parent-only descendant catalog/history remains the trace surface, including live workflow-member navigation through catalog-derived direct-parent addresses. Loaded catalogs project child metadata into keyed Client state for authorized descendant aggregation without adding child ids to the ordinary list. An authorized `subagent.prompt` adds only the matching authenticated principal's mux streams to that addressed child, with prefix replay closing the cold-to-live event gap. Hidden child Sessions are deleted child-first with their cold parent, while ordinary fork descendants retain the explicit deletion refusal. Shipped Web standard, Code, and Cordis Agent presets bind Profile management and expose the current parent grant beside its private in-memory revisions. |
| Runtime-context snapshot publication | `dsh-context-snapshot` (new; log-derived retained-state fold and publication policy over owned durable messages) | `dsh-context-snapshot` | The `dsh-system-prompt` contexts plane, published as durable user-role Session messages every model request reads | Complete ownership move: `dsh-agent-loop` no longer computes or appends runtime context. Complete snapshots publish at session start, after compaction, and on section-name-set changes; partial snapshots carry only changed sections with durable `form: 'snapshot', partial: true`; three recovery paths cover step, request-waterfall, and idle manual-compact boundaries. Enabled by `dsh-base` directly after `system-prompt`; `dsh-llm` and the Web conversation captions distinguish partial records. Implemented in `d745bf399c`. |
| Whole-surface context reset | `dsh-context-reset` (new; `ctx.contextReset` whole-surface replacement service owning the log-only `reset/checkpoint` anchor and the verbatim marker) | `dsh-context-reset` | `dsh-command-reset` (new; global argument-free `/reset`), and the scheduled-task delivery engine (planned) through the same service seam | One atomic anchor+marker pair shadows every current surface node under the existing surface-fold validation; no summary, no lock bracket, no core change. Idle-only via the `runMaintenance` phase with `busy | cancelled | commit | persistence` classification; display history recognizes the `reset` plugin in the same checkpoint set as `compact`, so initial pages cut at the reset anchor. Enabled by `dsh-base` beside the command adapter. Implemented in `fbd6ff2bfc`; windowed-resume folding landed in `abed1dfbcf` and `05d86b7c02`, and the invariant moved to append-rejecting enforcement in `3f5543c13e`. |
| Host-level durable scheduler | `dsh-scheduler` (new; `ctx.scheduler` over one central storage-domain store with at/after/every rules, wall-clock timers, per-schedule dispatch chains, prompt provenance, and durable run history) | `dsh-scheduler` | The delivery envelope (each plugin-source `user/message` carries schedule id, rule, planned/fired moments, and next run around the verbatim prompt, with the same facts structured on the message source; the former `schedule:pending` runtime-context section was removed so lifecycle writes never perturb the context-snapshot state machine) plus the preset-scoped `dsh-tool-scheduler` model tools (`schedule_create/list/delete/update`, with `schedule_update` limited to prompt and pause/resume status); the session-scoped Typert Remote (`list`/`runs`/`create`/`update`/`delete` via `dsh-api-remotes`) composes the same methods, and the host-authority management Remote (`listAll`/`runsOf`/`updateAny`/`deleteAny`) feeds the global center view over every stored schedule | Hot delivery through the idle maintenance phase, cold delivery through the session-delivery-local resume sequence with idle recycling, optional pre-delivery `ctx.contextReset` fresh runs, and lazily created reusable job sessions. One log-only `schedule/dispatch` provenance event precedes each plugin-source `user/message`. Enabled by `dsh-web-app` after the storage rows; the inherited opt-in official `dsh-schedule` was retired with its package in the wave-3 W02 retirement (the event-vocabulary removal is recorded there). Implemented in `9b60efd8c7`; the preset-scoped tool split landed in `1e28b9804c`, the session-scoped Remote and session-header UI in `b94a67823f`, the `delete` verb rename in `b2cabd2ae9`, append-rejecting invariant enforcement in `3f5543c13e`, and the workspace management UI, prompt provenance, and durable run history in `1642eecaae`. Session-bound delivery targets, rule edits, and the global management Remote landed in `f03d573d91`; `schedule_update` in `108c1bcbca`; the `center.view` frame seam (conversation stays mounted, inert, under a named full-center view) in `bdbde30798`; the footer-triggered global management view replacing the settings section in `949c69b901`; and its keyless e2e coverage in `9092100780`. The `schedule:pending` section was retired in favor of the delivery envelope in `9f1e0682f2` (see the scheduler-context Agent Note). |
| Context management suite | `dsh-compaction-basic` (route-fit pre-flight; modified), `dsh-context-nudge` (new; threshold-gated non-waking pressure notices through `agent.inject()`, token-only policy with refire delta and hysteresis, tool-catalog gate), `dsh-tool-compaction` (modified; explicit `from`/`to` spans with pairing snap, closed-turn cap, and retained-context feedback), `dsh-context-inspector` (new; read-only `ctx.contextInspector.manifest()` reusing the loop's assembly primitives with log-seq provenance) | `dsh-compaction-basic`, `dsh-context-nudge`, `dsh-tool-compaction`, `dsh-context-inspector`, `dsh-compaction-settings` (nudge fields), `dsh-client-ui-settings-plugins` (card fields) | Layered fit-to-window recovery before a doomed request (model switches, silent-truncation locals), model-initiated span selection, absolute-threshold pressure notices, and an auditable next-request manifest. The nudge notice records `measuredTokens`/`thresholdTokens` on its source and its invariant rejects below-threshold records. Implemented in `9d99f4e479` (route-fit), `87e8be2ee2` (nudge + settings), `530913b7d8` (explicit spans), and `d19bbbfe51` (inspector); see the context-management Agent Note. The inspector's Web consumer is the Trajectory view's `Current context` strip (`dsh-client-ui-trajectory`): a live block band under the ledger, one block per model-visible item with landed compactions in a distinct style, each click navigating the ledger to the item's record; its CLI consumer is `/context` (`dsh-command-context`, new) rendering the manifest with per-segment seq provenance. |
| Resource governor | `dsh-governor` (new; `ctx.governor` — a `TypertRemoteService` over the subprocess seam's metering events, a `/proc` sampling engine, a cgroup-v2 enforcement tier with an injectable filesystem abstraction, and a shared-pool quota book backed by the `governor` storage domain) | `dsh-governor` | The model-facing `resource-quota` tool, mounted through the separately loadable `dsh-governor/tool` Consumer (preset-scoped in the shipped Web roster; own-session quota negotiation; raises pass admission and one approval under the `ask` policy); the `governor` Typert Remote namespace (`overview`/`sessionSamples`/`sessionQuotaGet`/`breaches`/`configGet` under `harniverse.observe`, `sessionQuotaAdjust` under `harniverse.operate`, `reload` under `harniverse.administer` via `dsh-api-remotes`); `dsh-tool-bash` stamps every bash call's correlation and merges breach facts into result meta; `dsh-bash-local`/`dsh-terminal-bash` forward the correlation to the subprocess seam; `host.describe` publishes the resolved tier | Metering rides `subprocess/spawned`/`terminal-spawned` events (unstamped spawns stay exempt: LSP, subagents, ripgrep). Tier C (writable cgroupfs) enforces the global budget on a `dsh/` parent and explicit quotas on `dsh/<sessionId>/` leaves asynchronously; tier R (read-only cgroupfs) fronts non-PTY shell spawns with `prlimit --as=` and a sampler watchdog kills the largest offender after sustained overage; tier O observes. Overriding quotas are durable storage-domain rows replayed first-come through admission on restart (clamping notifies the agent), explicit closes drop rows, and a TTL sweep collects abandoned ones; every change appends a log-only `governor/quota` audit event. Enabled by `dsh-web-app` after the scheduler row. |
| Harness checkout context | `dsh-harness-source` (new; checkout-root derivation and `harness:source` registration) | `dsh-harness-source` | `dsh-web-app` | Complete ownership move: the order-−99 single-paragraph context names `HARNESS_SOURCE_ROOT` and preserves the pwd/cwd separation sentences; the DSH relationship disclaimer moved into `dsh-system-prompt`'s exported `HARNESS_IDENTITY` opener (`includeHarnessIdentity: false` removes both), `dsh-app-boot`'s `addHarnessSourceContext` is deleted with no compatibility path, and `dsh-web-app` mounts the plugin in every Web composition while `surfaceContext` gates only `app:web-surface`. Implemented in `d745bf399c`. |
| Bounded control-channel transport | `dsh-control-channel` (modified; the wave-3 first-batch frame contract gains a multiplexed stdio transport for multi-request child processes) | `dsh-control-channel` | `dsh-ptc-runtime-node` and `dsh-ssh` | Complete transport layer for the frame/backpressure/lifecycle contract: one child stdio carries interleaved bounded requests with per-request cancellation, ordered release under backpressure, and terminal loss semantics that invalidate every outstanding request; the PTC runtime and the SSH helper share it unchanged. Transport implementation: `6ede621565`. |
| Fresh-process PTC runtime | `dsh-ptc-runtime` (`PtcRuntime` Service Definition, exposed as `ctx.ptcRuntime`) | `dsh-ptc-runtime-node` (fresh-process Node Provider with explicit Node, source/built bootstrap, pkg and Electron self-execution), `dsh-ptc-runtime-python` (opt-in CPython Provider), and `dsh-subprocess-local` (child-process transport) | PTC tool presentation (`dsh-tools`, `dsh-agent-tool-presentation`), headless and Web bundles, `dsh-spill-policy` tests, Python SDK packaging, and the Desktop Host | W09 naming and bootstrap implementation is complete. Active packages, service access, composition and PTC SDK copy use PTC terminology; durable `code` preset ids, `run_code` and event contracts remain stable. Node execution stays in a fresh child over the bounded control channel; Electron selects Node mode and removes bootstrap-only environment values before descendants inherit them. |
| Desktop-owned authenticated Host | Existing `dsh-authentication`, `dsh-directory-picker`, `dsh-host-webserver`, session, terminal and scheduler service contracts | App-local Desktop Host HTTP admission and native directory-picker Providers, plus the existing authenticated Web composition | `apps/desktop-host` control Consumer and `apps/desktop` shell using the shared Web client | W16 source implementation is complete. Private exact-key bootstrap restores the recorded Grant, browser partitions separate owned and external origins, a dedicated executable Profile owns the dependency closure, and there is no OS login autostart registration. After login the user launches the app and selects the local Host; schedules run only while it runs. Close hides while work continues, logout/reboot/Quit stop the owned Host, and explicit disconnect/Quit warns with retry while work is active or unknown but can release a confirmed-dead Host even after an unclean exit. Native update transactions retain the stricter acknowledged zero-status exit requirement, require consent and idle owned work, preserve recovery records, and never control a separately owned Host. Final branded Linux x64 AppImage and unpacked qualification passed locally with inventory `0acb0a809a1433abc933c47862604401b2c911cb68548654fd4204c86db7518f`; Windows/macOS CI qualification remains pending. |
| Desktop-bundled Host browser payload | `dsh-api-browser-controller` (`ctx.browserController`) | The existing Host Chromium Provider configured with the packaged executable | `dsh-client-ui-browser` and the Desktop Host composition | The production distribution carries the complete pinned Playwright 1.61.1 Chrome for Testing payload (Chromium revision 1228, browser version 149.0.7827.55). `runtime-input.json` and `offline-assets.json` require browser executable/version/revision/Playwright metadata; the packaged Profile validates the anchored path before boot and sets `executablePath` with `sandbox: 'auto'`. Final branded Linux x64 AppImage and unpacked qualification passed with inventory `0acb0a809a1433abc933c47862604401b2c911cb68548654fd4204c86db7518f`, a 12,079-byte JPEG, correct title, one local request, zero pages after close and acknowledged exit 0; Windows/macOS CI qualification remains pending. |
| Captured POSIX SSH execution world | `dsh-ssh` (new; OpenSSH connection with helper digest verification, bounded RPC over the shared control-channel transport, lease heartbeats, the immutable `WorldDescription` carrying machine-owned MCP/Skill/Hook inventory and captured Profile restrictions, and the deployment-owned bootstrap pairing the PTC runtime reuses) | `dsh-fs-ssh`, `dsh-subprocess-ssh`, `dsh-sandbox-ssh` (new; remote `ctx.fs`/`ctx.subprocess`/`ctx.sandbox` providers over one shared connection) | Opt-in remote deployments only; no shipped Web composition mounts the family | Complete remote execution seam: helper installation and cleanup join with lease-heartbeat expiry, transport loss invalidates the connection, machine-owned tools/resources/templates/Skills/Hooks are enforced directly, `cordis` and live Host authority stay excluded, and an isolated local sshd fixture covers owned keys, digest failure, reconnect, filesystem, subprocess, PTY, and resize. Implemented in `7cd693646b`. |

### Added Packages

| Package | Role | Default composition |
|---|---|---|
| `@deepseek-ai/dsh-authentication` | Inbound authentication Service Definition | Type/service dependency; the concrete Web row loads its Provider. |
| `@deepseek-ai/dsh-authentication-local` | Local public-key Grant, challenge, Access Token, and browser-session Service Provider | Enabled by `dsh-web-app`. |
| `@deepseek-ai/dsh-client-authentication` | Client authentication Service Definition and browser Provider | Adopted as kernel infrastructure from the lightweight authentication entry, with the same runtime and one plugin-owned disposal. |
| `@deepseek-ai/dsh-client-ui-connection-status` | Read-only connection-health Consumer in `sidebar.header.status` | Enabled by `dsh-web-app`; the sidebar owns layout only. |
| `@deepseek-ai/dsh-auth-app` | One-shot authentication management bundle and Consumer | Shipped as the standalone `auth` profile; `dsh auth` is its alias. |
| `@deepseek-ai/dsh-notification` | Notification Service Definition plus lifecycle projection Consumer | Not mounted alone. |
| `@deepseek-ai/dsh-notification-http` | Durable HTTP/HTTPS notification Service Provider | Opt-in examples only. |
| `@deepseek-ai/dsh-tool-result-artifacts` | Finalized-result retention and model-facing retrieval Consumer | Enabled in base/headless scopes and per-agent Web presets. |
| `@deepseek-ai/dsh-compaction-lossless` | Automatic compaction Provider plus committed summary-DAG projection | Enabled in base, the standalone headless example, and the standard, code, and Cordis presets. |
| `@deepseek-ai/dsh-compaction-settings` | Root-owned live `compaction` Settings namespace for an optional global pressure threshold | Enabled by `dsh-base`; registers no compaction service and leaves Profile thresholds intact while unset. |
| `@deepseek-ai/dsh-tool-compaction` | Direct model-facing Consumer for one retained-tail compaction request | Enabled beside `dsh-compaction-lossless` in base, standalone headless, standard, and Cordis; omitted by minimal and Code presets. |
| `@deepseek-ai/dsh-tool-compaction-history` | Bounded current-session summary search and expansion Consumer | Enabled beside `dsh-compaction-lossless`; omitted by the minimal preset. The headless overflow snapshot pins its model-visible names and safety guidance. |
| `@deepseek-ai/dsh-plugin-diagnostics` | Effect-scoped read-only diagnostic registry and report coordinator | Enabled by `dsh-web-app` before diagnostic Providers and the Host Remote. |
| `@deepseek-ai/dsh-plugin-diagnostics-cordis` | Host Loader, standing preset, and dynamic Cordis diagnostic Provider | Enabled by `dsh-web-app`; contributes observations only and owns no repair operation. |
| `@deepseek-ai/dsh-capabilities` | Scoped recipe registry, inherited Agent composition, dependency planner, and generation selection coordinator | Enabled by `dsh-base`; registers no model-facing capability by itself. |
| `@deepseek-ai/dsh-host-capability-management` | Static Profile-recipe and Host-provider adapters plus authorized catalog/plan/apply/Session Remote | Enabled by `dsh-web-app`; observation requires `harniverse.observe`, mutation requires `harniverse.administer`. |
| `@deepseek-ai/dsh-mcp-user-config` | Settings-owned multi-server MCP Provider/Consumer bridge | Provider enabled by `dsh-base`; scoped consumers enabled by the Standard, Code, and Cordis Profiles; Minimal omits the consumer. |
| `@deepseek-ai/dsh-supervision` | Durable per-session supervision mode Service and model-context/UI projection | Enabled by `dsh-base`; Profile and Child Profile metadata select the initial mode. |
| `@deepseek-ai/dsh-model-policy` | Independent Model Profile/Route settings service, Session snapshots, and concrete-target authorization | Enabled by `dsh-base`; built-in `unrestricted` covers legacy Sessions. |
| `@deepseek-ai/dsh-model-policy-fallback` | Session-aware ordered cross-model fallback executor | Enabled by `dsh-base`; same-model retry remains owned by `dsh-llm-retry`. |
| `@deepseek-ai/dsh-client-ui-settings-capabilities` | Global/Profile assembly editor, plan preview, and read-only Session capability view | Enabled by `dsh-web-app` as a Plugins Settings tab and conversation view. |
| `@deepseek-ai/dsh-client-ui-reference` | Web file/session `@` candidate Consumer | Enabled by `dsh-web-app`. |
| `@deepseek-ai/dsh-ptc-runtime-python` | Fresh-process CPython `PtcRuntime` Provider | Opt-in; no shipped Profile selects it. |
| `@deepseek-ai/dsh-file-reference` | Bounded path-discovery Definition and Remote export | Enabled by `dsh-web-app` through the local Provider. |
| `@deepseek-ai/dsh-file-reference-local` | Local workspace-indexing file-reference Provider | Enabled by `dsh-web-app`. |
| `@deepseek-ai/dsh-session-delivery` | Provider-neutral session create/message/unload Definition | Enabled by `dsh-base` through the local Provider. |
| `@deepseek-ai/dsh-session-delivery-local` | Local ordinary/direct-child session-delivery Provider | Enabled by `dsh-base`. |
| `@deepseek-ai/dsh-tool-session-delivery` | Model-facing session create/message/unload Consumer | Enabled by `dsh-base`; shipped Standard, Code, and Cordis Profiles expose its tools. |
| `@deepseek-ai/dsh-tool-pwsh-persistent` | Persistent PowerShell tool Consumer over the Terminal service | Opt-in examples only; no shipped base/Profile composition. |
| `@deepseek-ai/dsh-web-search-tavily` | Tavily Search provider for `ctx.web` | Mounted by `dsh-base`; model-facing tools remain disabled there by default. |
| `@deepseek-ai/dsh-web-search-brave` | Brave Search provider for `ctx.web` | Mounted by `dsh-base`; model-facing tools remain disabled there by default. |
| `@deepseek-ai/dsh-web-search-kagi` | Kagi Search provider for `ctx.web` | Mounted by `dsh-base`; model-facing tools remain disabled there by default. |
| `@deepseek-ai/dsh-web-firecrawl` | Firecrawl aggregate Search/Scrape provider for `ctx.web` | Mounted by `dsh-base`; model-facing tools remain disabled there by default. |
| `@deepseek-ai/dsh-http-proxy` | Process-wide outbound proxy policy, absorbed natively from official `util/http-proxy`: resolves `HTTP(S)_PROXY`/`ALL_PROXY`/`NO_PROXY` from the launch-environment snapshot, installs a hand-rolled dispatcher behind the symbol global `fetch` resolves, and exposes the one shared proxy-hop builder | Library, not a plugin; `dsh` profile boot installs it before the first plugin mounts. |
| `@deepseek-ai/dsh-context-snapshot` | Runtime-context snapshot publication for the system-prompt contexts plane | Enabled by `dsh-base` directly after `system-prompt`; the emission formerly inside `dsh-agent-loop` moved here. |
| `@deepseek-ai/dsh-harness-source` | `harness:source` checkout-root dynamic context | Enabled by `dsh-web-app` in every Web composition; replaces the removed `dsh-app-boot` helper. |
| `@deepseek-ai/dsh-context-reset` | Whole-surface context-reset service (`ctx.contextReset`) owning the `reset/checkpoint` anchor and replacement marker | Enabled by `dsh-base`; display history cuts initial pages at the reset anchor. |
| `@deepseek-ai/dsh-command-reset` | Human-facing `/reset` command over the context-reset seam | Enabled by `dsh-base` beside `context-reset`; cold-discoverable, idle-only, argument-free. |
| `@deepseek-ai/dsh-scheduler` | Host-level durable scheduler (`ctx.scheduler`) with central storage, hot/cold delivery, fresh-context runs, job sessions, named-session targets, and rule edits | Enabled by `dsh-web-app` after the storage rows; registers no model tools itself. |
| `@deepseek-ai/dsh-tool-scheduler` | Preset-scoped `schedule_create` / `schedule_list` / `schedule_delete` / `schedule_update` tools over `ctx.scheduler` | Selected by the `standard`, `code`, and `cordis` preset rows; pending on compositions without the service, so `minimal` keeps its two-tool contract. `schedule_update` edits prompt and pause/resume status only; rule and target rebinding stay human-only in the management view. |
| `@deepseek-ai/dsh-client-ui-scheduler` | Session-header schedule list plus the global schedules management center view (sidebar footer trigger, full-center table with create/edit drawer and run history) over the scheduler Remote | Enabled by `dsh-web-app` after `ui-jobs`; the header renders only when the session owns schedules, while the center view lists every stored schedule through the host-authority Remote and occupies the new `center.view` frame slot. |
| `@deepseek-ai/dsh-governor` | Resource governor (`ctx.governor`): per-command CPU/memory/disk/network-lite metering for correlated shell/terminal spawns, tiered memory enforcement (cgroup / prlimit+watchdog / observe), and shared-pool session memory quotas with durable overrides and the `resource-quota` model tool | Enabled by `dsh-web-app` after the scheduler row; exposes the `governor` Typert Remote namespace and consumes the storage-domain `governor` unit. |
| `@deepseek-ai/dsh-client-ui-governor` | Panel (会话看板) shell over the governor Remote: a sidebar footer trigger, an in-page tab ring over `governor.center.tab` contributions (hidden with a single contribution), the built-in Resources tab (tier badge, global budget bar, per-session CPU/memory/command/breach rows, inline quota negotiation, host disk/network sentinels), plus the Resource-governance settings section (global memory budget `auto`/GiB through the settings scope, effective value from `configGet`) | Enabled by `dsh-web-app` after `ui-scheduler`; polls `overview` at the sampling cadence so the page is the same HTTP API surface scripts use, and takes `@deepseek-ai/dsh-client-ui-settings` for the settings-section registration. |
| `@deepseek-ai/dsh-queue` | Session message queue (`ctx.queue`): Kafka-style durable topics with dense per-topic offsets, time-based forced archival (expired or archived messages are never delivered, even to lagging subscribers), a silent bidirectional subscription relation model (deleting either side dissolves the relation immediately), and wake-on-deliver fan-out — idle subscribed sessions wake through `agent.followup`, running ones receive the injection after the blocking command | Enabled by `dsh-web-app` after the governor row; exposes the `queue` Typert Remote namespace (`topicList`/`messages`/`subscriptions`/`stats` under `harniverse.observe`; `topicCreate`/`topicDelete`/`publish`/`subscribe`/`unsubscribe` under `harniverse.operate`) and consumes the storage-domain `queue` unit. |
| `@deepseek-ai/dsh-queue/tool` | Separately loadable Consumer registering the four queue model tools: `queue-topic` (list / dual-direction inspect / silent delete), `queue-history` (past-tense cursor-free read), `queue-subscription` (calling-session-only subscribe/unsubscribe), `queue-publish` (per-message TTL override) | Mounted by the shipped `standard` preset's `tool-queue` row; minimal preset stays without it. |
| `@deepseek-ai/dsh-client-ui-queue` | Message-queue tab of the panel (会话看板) over the queue Remote: topic table with aggregates, one topic's live/archived message history, the subscription relation with dormant badges, and capability-gated publish/subscribe controls | Enabled by `dsh-web-app` after `ui-governor`; polls the queue Remote at the sampling cadence. |
| `@deepseek-ai/dsh-ptc-runtime-node` | Fresh-process Node `ctx.ptcRuntime` Provider: a platform-correct child spawn plan (`nodeExecutable`/`bootstrapPath` config, pkg and Electron self-execution) over the control-channel transport | Selected by PTC compositions in headless and Web bundles, `dsh-spill-policy` tests, Python SDK runtime packaging and the Desktop Host. |
| `@deepseek-ai/dsh-ssh` | Captured POSIX SSH execution world: verified helper, bounded control-channel RPC with lease heartbeats, machine-owned MCP/Skill/Hook inventory, and the immutable `WorldDescription` | Opt-in remote deployments; no shipped composition mounts it. |
| `@deepseek-ai/dsh-fs-ssh` | Remote `ctx.fs` Provider over one shared SSH connection | Opt-in remote deployments; no shipped composition mounts it. |
| `@deepseek-ai/dsh-subprocess-ssh` | Remote `ctx.subprocess` and terminal Provider (streaming stdio, PTY, resize) over one shared SSH connection | Opt-in remote deployments; no shipped composition mounts it. |
| `@deepseek-ai/dsh-sandbox-ssh` | Remote `ctx.sandbox` confinement Provider over one shared SSH connection | Opt-in remote deployments; no shipped composition mounts it. |
| `@deepseek-ai/dsh-deque` | Zero-dependency circular deque for queues that retain entries across asynchronous work | Library, not a plugin; consumed by `dsh-api-terminal-controller` follower queues. |
| `@deepseek-ai/dsh-api-terminal-controller` | Session-owned interactive USER terminal controller (`ctx.terminalController`) over the subprocess PTY seam: idempotent per-Session create, headless-xterm screen recovery, single-controller attachment fencing, unattended reclamation; full harness environment, login-shell startup, never sandbox-confined, never model-visible | Enabled by `dsh-web-app` beside the API gateway; the `terminal/*` Remote endpoints ride the gateway and the `events.terminal` / `events.hold` SSE streams ride the apiproxy EventsApi carrier. |
| `@deepseek-ai/dsh-client-ui-browser` | Host-driven browser carrier section of the Workspace workbench: renders the JPEG screencast frames the host browser controller streams over `events.browser`, forwards pointer, wheel, and keyboard input through the `browser/*` gateway verbs, and owns the tab strip, address bar, and history navigation (back/forward/reload through the host's own navigation history); registers its tab and body through `workbench.section.tab`/`workbench.section.panel` beside the shipped Files/Changes/Search sections | Enabled by `dsh-web-app` after `ui-governor`; a human-only carrier — no tools, no session events, nothing model-visible, and page egress originates from the host process rather than the user's device. |
| `@deepseek-ai/dsh-client-ui-terminal` | Terminal section of the Workspace workbench over the `terminal/*` gateway verbs and the `events.terminal`/`events.hold` streams: xterm.js mount with fit-driven resize, exclusive input attachment with read-only followers, and a bounded slow-follower re-attach ladder that recovers by snapshot-first re-attach; registers its tab and body through `workbench.section.tab`/`workbench.section.panel` beside the shipped Files/Changes/Search sections | Enabled by `dsh-web-app` after `ui-browser`; a human-only surface — terminal traffic never enters the Session log. |

### Modified Official Plugin Families

| Area | Official packages changed by Harniverse |
|---|---|
| PTC runtime naming and bootstrap (W09) | `dsh-ptc-runtime` (renamed Service Definition), `dsh-ptc-runtime-node` and `dsh-ptc-runtime-python` Providers, `dsh-tools`, `dsh-agent-tool-presentation`, preset/settings UI, shipped compositions and SDK packaging; active references use `PtcRuntime` / `ctx.ptcRuntime` and PTC SDK vocabulary while durable preset and tool contracts remain stable |
| Model and Web defaults | `dsh-base`, `dsh-web`, `dsh-web-fetch-http`, `dsh-web-firecrawl`, `dsh-web-search-brave`, `dsh-web-search-deepseek`, `dsh-web-search-exa`, `dsh-web-search-kagi`, `dsh-web-search-perplexity`, `dsh-web-search-tavily`, `dsh-client-ui-settings-models`, `dsh-client-ui-settings-plugins` |
| Session control and reconnect | `dsh-agent`, `dsh-agent-loop`, `dsh-session`, `dsh-host-apiproxy`, `dsh-client-connection`, `dsh-client-runtime`, `dsh-session-persistence`, `dsh-session-persistence-jsonl`, `dsh-session-persistence-sqlite`, `dsh-session-projection-cache`, `dsh-workspace` |
| Agent Profile identity and composition | `dsh-agent-presets`, `dsh-host-apiproxy`, `dsh-permission-presets`, `dsh-client-runtime`, `dsh-client-ui-agent-preset`, session persistence/query Providers, and in-process subagent composition |
| Result retention and bounded file access | `dsh-tools`, `dsh-spill`, `dsh-spill-local`, `dsh-spill-policy`, `dsh-tool-fs`, `dsh-tool-fs-search`, `dsh-tool-str-replace-editor`, `dsh-client-ui-tool`, `dsh-compaction`, `dsh-token-meter` |
| Compaction composition, proactive trigger, and recall | `dsh-compaction`, `dsh-compaction-basic`, `dsh-command-compact`, `dsh-base`, `dsh-web-app`, `dsh-host-apiproxy`, `dsh-client-ui-settings-plugins`, and the standalone headless example; standard and Cordis Agent presets select both model Consumers, Code keeps automatic compaction and recall without the direct-only trigger, and Minimal selects only the internal Provider/summary-DAG projection. Request pressure snapshots exact-model/global/Profile threshold priority before capacity lookup, reserves output capacity before provider dispatch, retries reduced output caps before forced compaction, and manual compaction waits for active agent turns to settle. The global command remains cold-discoverable while execution and teardown follow the invoking Profile's scoped Provider. Auxiliary summary calls project matching Provider usage onto the selected prefix and bound output by policy, conversation cap, model output capability, configured request default, remaining context, and a safety reserve. |
| Authenticated Web and automation surface | `dsh-web-app`, `dsh-client-connection`, `dsh-client-modules`, `dsh-client-hmr`, `dsh-client-web`, `dsh-host-webserver`, `dsh-host-frontend-static`, `dsh-sdk-client`, `dsh-api-gateway`, `dsh-typert-protocol`, `dsh-typert-generator`, `dsh-typert-loader`, `dsh-typert-registry`; the browser shell renews short sessions at half-life, retries transient exchange failures across Cookie expiry, wakes on focus/visibility/network recovery, and admits ordinary authenticated pages only with a renewable device key or explicit loopback bypass. |
| Workspace inspection transport and UI | `dsh-host-apiproxy`, `dsh-api-remotes`, `dsh-client-connection`, `dsh-client-runtime`, `dsh-client-ui-conversation`, `dsh-client-ui-layout`, `dsh-client-ui-slots`, `dsh-client-ui-workspace`, `dsh-test-support-client-runtime` |
| DeepSeek provider-local multimodal requests | `dsh-attachment`, `dsh-attachment-local`, `dsh-llm-deepseek` |
| Derived runtime catalogs | `dsh-cordis-client-runner`, `dsh-tool-cordis` |
| Test support projection | `dsh-acp-snapshot` normalization only |
| Local process confinement | `dsh-sandbox-local`, `dsh-bash-sandbox` |
| Platform-native shell defaults and runtime packaging | `dsh-shell`, `dsh-bash-local`, `dsh-bash-sandbox`, `dsh-terminal-bash`, `dsh-tool-bash-persistent`, and Python runtime packaging scripts |
| Plugin operations and diagnostics | `dsh-host-plugin-inventory`, `dsh-api-remotes`, `dsh-client-ui-settings-plugin-inventory`, `dsh-web-app` |
| Agent Profile capability composition | `dsh-agent-presets`, `dsh-tools`, `dsh-mcp-client`, `dsh-cordis-host-runner`, `dsh-tool-cordis`, `dsh-api-remotes`, `dsh-web-app` |
| Supervision policy and Profile selection | `dsh-supervision`, `dsh-agent-presets`, `dsh-host-apiproxy`, `dsh-subagent`, `dsh-user-questions`, `dsh-user-approval`, `dsh-plan-mode`, `dsh-client-ui-conversation` |
| User extension defaults and Hook bridges | `dsh-hooks-claude-code`, `dsh-hooks-codex`, `dsh-hook-protocol`, `dsh-mcp-user-config`, `dsh-mcp-client`, `dsh-web-fetch-http`, `dsh-web-firecrawl` |
| Parent-private Child Profile foundation | `dsh-subagent`, `dsh-subagent-in-process-driver`, `dsh-subagent-dsh-sdk`, `dsh-tool-subagent`, `dsh-tool-subagent-control`, `dsh-client-ui-trajectory` |
| Shared workspace storage compatibility | `dsh-storage`, `dsh-storage-domain`, `dsh-storage-json`, `dsh-storage-sqlite`, `dsh-workspace` |
| Prompt identity and runtime-context ownership | `dsh-system-prompt`, `dsh-agent-loop`, `dsh-app-boot`, `dsh-web-app`, `dsh-llm`, `dsh-client-ui-conversation` |
| Asynchronous provider preparation and remote POSIX execution | `dsh-shell`, `dsh-bash-local`, `dsh-pwsh-local`, `dsh-bash-sandbox`, `dsh-pwsh-sandbox`, `dsh-tool-bash`, `dsh-tool-pwsh`, `dsh-sandbox`, `dsh-sandbox-local`, `dsh-subprocess`, `dsh-subprocess-local`, `dsh-terminal-bash` — executor `start()` and sandbox `confine()` accept asynchronously prepared providers through additive union returns, bash/pwsh background jobs keep cancellation across asynchronous startup, terminal resize rides the subprocess contract, and the local PTY owns its resize path |
| Authenticated browser terminal surface (W13) | `dsh-typert-protocol` (structural `RemoteError` + `remoteErrorOf`, gateway passthrough), `dsh-host-apiproxy` (`terminal-unavailable`/`terminal-control-unavailable`/`terminal-limit-reached` carrier codes; `events.terminal` / `events.hold` SSE stream faces and fetch routes under `harniverse.observe`), `dsh-client-connection`/`dsh-client-runtime` (EventsApi contract re-exports and fake surfaces) — stream abort detaches the attachment or releases the window hold; terminal content never enters the Session log |
| Host-driven browser carrier surface (W14) | `dsh-api-browser-controller` (new; host Chromium over CDP with zero new dependencies) owns the pages: per-Session one-time profile directories, `sandbox: 'auto' | 'chromium' | 'none'` resolving the Chromium sandbox against the harness's own privilege (`'auto'` drops it only where Chromium cannot start with it — a root harness — so a container deployment has a usable panel), launch failures reported as `browser-unavailable` carrying the browser's own stderr, navigation policy at the CDP boundary (http/https only, embedded-credential and non-network scheme refusal), JPEG screencast frames with input dispatch; `dsh-host-apiproxy` carries the `browser-unavailable`/`browser-control-unavailable`/`browser-limit-reached`/`browser-navigation-refused` codes and the `events.browser` SSE stream face under `harniverse.observe`; `dsh-client-ui-browser` renders frames and forwards input; `dsh-client-ui-conversation` routes markdown links into the section through `ILayout.openWorkbenchSection(section, request?)` (the upstream `openExternalLink` seam) under the durable `ui-conversation.linkDestination` preference — `'panel'` by default, `'device'` declining the click so the anchor opens it in the reader's own browser, which is also the answer where the host has no browser program — a human-driven carrier whose page egress comes from the host, never the user's device, with no page-probing or agent-scraping surface |
| Full-access ambient environment inheritance | `dsh-subprocess` (spec-level `ambientEnv: 'full' | 'scrubbed'`), `dsh-subprocess-local` (`childEnv` ambient switch), `dsh-bash-local`/`dsh-pwsh-local` (stamp `'full'` under `danger-full-access`), `dsh-api-terminal-controller` (user terminals spawn `'full'` and request `term: 'xterm-256color'`) — the model PTY `dsh-terminal-bash` keeps the scrubbed base and the seam's capability-free `term` default, so model-visible processes never silently receive credentials and no escape sequences enter the transcript |
<!-- capability-changes-end -->

## Shipped Composition Changes

<!-- composition-changes-start -->
| Composition surface | Current Harniverse change |
|---|---|
| PTC runtime and presentation (W09) | Active package paths and manifests use `ptc-runtime`, `ptc-runtime-node` and `ptc-runtime-python`; `PtcRuntime` / `ctx.ptcRuntime` are the service contract. UI and SDK descriptions use PTC. Durable `code` preset ids, `run_code` and stored event vocabulary are unchanged. Node Provider bootstrap supports source, built, pkg and Electron launches without forwarding bootstrap-only environment values to descendants. |
| Desktop Web profile and native ownership (W16) | `apps/desktop-host` loads the distribution-owned authenticated Web bundle tuple in a dedicated home and temporary Loader root, with app-local admission and native directory-picker Providers plus a control Consumer of existing services. It does not load user executable profile patches or install packages at runtime. `apps/desktop` presents the shared client, keeps owned and external browser partitions separate, persists the P-256 device, and controls only its own child. There is no OS login autostart; after login the user launches the app and selects the local Host, whose schedules run only while it is running. Window close hides; logout/reboot/Quit stop the owned Host; explicit Quit/disconnect warns and offers retry for active or unknown work, while a confirmed-dead Host may be released after an unclean exit. The common blue-H mark supplies native window/tray and generated PNG/ICO/ICNS branding; final branded Linux x64 artifact qualification passed locally and Windows/macOS CI remains pending. |
| Desktop bundled browser composition (W16) | The existing `dsh-api-browser-controller` Definition/Provider remains the Host page owner. The packaging seam provisions pinned Playwright 1.61.1 Chromium revision 1228 / Chrome for Testing 149.0.7827.55 and records `browser: { executable, version, revision, playwrightVersion }` in both runtime manifests. The Desktop Profile reads and validates the manifest-anchored executable before boot, then configures `browser-controller` with `executablePath` and `sandbox: 'auto'`; the existing browser UI remains the human-only Consumer. Final branded Linux x64 AppImage and unpacked artifact checks passed with inventory `0acb0a809a1433abc933c47862604401b2c911cb68548654fd4204c86db7518f`, a 12,079-byte JPEG frame, correct title, one local request, zero pages after close, acknowledged exit 0 and empty-path auth/lifecycle smoke. Windows/macOS CI qualification remains pending. |
| Desktop whole-unit updates (W16) | Native local-artifact selection validates identity/version/target/digest and requests explicit consent. Owned work must pass inspect-lock-inspect idle checks before acknowledged shutdown. Atomic journals preserve recovery; writable AppImage replacement retains verified old bytes and supports bounded restoration, while Windows NSIS/macOS DMG use native handoff without automatic installer rollback. No update feed or publisher authentication is implied by the manifest checksum. Linux x64, Windows x64 and macOS arm64 artifacts and CI remain qualification work; additional CPUs are declarative until verified. |
| Vendored Cordis namespace | Vendored framework packages publish under the `@deepseek-ai` scope, and harness module specifiers use those names. Stable product and protocol identifiers remain independent: the `cordis` Agent Profile id, `cordis/*` events, diagnostics ids, and translation keys are not npm subpaths. The rescope gate preserves those distinctions while requiring every actual package reference to use the downstream namespace. |
| Vendored Cordis exact-config HMR | `@deepseek-ai/cordis-plugin-hmr` owns an initial stat baseline and asynchronous exact-path polling for `registerConfig`, independent of native watcher startup and module roots. Polling uses `interval` (default 100 ms), refreshes existing files once, and coalesces later observed changes. Cordis effects own registration and quiescent disposal; serialized refreshes, failure broadcasts, and the main Chokidar module/Include watcher remain intact. The [readiness decision](.agents/notes/implemented/bug-fix/2026-09-11-ci-readiness-boundaries.md) records timing, errors, and verification. |
| `dsh-base` model adapters | Native `dsh-llm-deepseek` remains installed but defaults disabled; official `dsh-llm-pi-ai` is the enabled vendor-neutral adapter. |
| `dsh-base` Web providers | DeepSeek, Exa, Perplexity, Tavily, Brave, and Kagi provide search; Firecrawl provides aggregate Search/Scrape; HTTP provides fetch. Search and fetch defaults are live settings-backed and explicit operation provider ids never fall back. |
| `dsh-base` model Web tools | `dsh-tool-web` remains loaded with both search and fetch disabled by default. |
| `dsh-base` result policy | Legacy `dsh-spill-policy` and `dsh-compaction-tool-result-pruner` rows default disabled; `dsh-tool-result-artifacts` handles finalized-result retention and `artifact_read`. |
| `dsh-base` compaction | `dsh-compaction-lossless` replaces the official basic row while inheriting its automatic transaction policy; `dsh-tool-compaction` exposes one direct retained-tail request and `dsh-tool-compaction-history` exposes bounded recall. The base owns the global `/compact` registration and root `compaction` Settings namespace; command execution resolves the scoped Provider selected by each Profile, and the Web Plugins page exposes the live global threshold as an integer percentage. Web moves the Provider and history Consumer behind standard, code, and Cordis Agent presets, moves the direct-only Consumer behind standard and Cordis, and gives Minimal only the hidden Provider/summary-DAG projection. Shipped compaction summaries use a 32,768-token cap and expose transient reasoning/summary progress to the authorized Web UI without adding progress records to Session history. |
| `dsh-base` user MCP settings | `dsh-mcp-user-config` owns the `mcp` settings namespace once. Standard, Code, and Cordis mount scoped consumers that actively load and disclose enabled user servers; disabled entries create no child client or model tool, and Minimal mounts no consumer. |
| Standard-family Hook defaults | Standard, Code, and Cordis keep both dialect bridges available but disabled by default. Explicitly enabled rows discover global and dialect-specific project Hook files per session event, merge configured layers, apply disabled entries, and refresh on the next event; Minimal mounts neither bridge. |
| Standard-family Web tools | Standard, Code, and Cordis explicitly enable `web_search` and hardened `web_fetch`. The local HTTP Provider rejects non-public DNS/literal targets and redirects using direct pinned transport; Firecrawl fetch remains opt-in, while the base host Web tool layer remains disabled. |
| Standalone headless example | The runnable headless composition selects the same lossless Provider, proactive Consumer, and history Consumer; its keyless overflow snapshot verifies committed replacement plus model-visible recall tools and untrusted-history guidance. |
| `dsh-web-app` authentication | `dsh-authentication-local` initializes before WebServer bind; connection injects the provider-neutral service. |
| `dsh-web-app` transport | Non-loopback listeners require direct TLS; authentication bypass is loopback-only. The Web startup provider carries explicit Host and exact-Origin trust into `dsh-client-connection`, while the composition prints the effective trust policy and mounts the Cordis console exporter for privacy-minimal connection and authentication diagnostics. Client-aborted request bodies close quietly; route failures that still have a response peer remain warnings. |
| `dsh-web-app` browser entry paths | The static fallback renders the shell only for `/`, the built index path, and the composition-owned `/auth/manage` entry. Missing assets and undeclared pathnames return an empty 404 rather than successful HTML. |
| `dsh-web-app` client plugin delivery | Initial browser boot registers every independent plugin factory through one revision-addressed, gzip-capable `/plugins/bootstrap.js` resource; per-plugin scripts and source maps remain available for aggregate fallback and HMR, preserving the plugin-native Loader/fiber lifecycle. |
| `dsh-web-app` transport encoding | The Host `/api` bridge negotiates `content-encoding` for buffered replies (Brotli, else gzip, else verbatim; `q=0` counts as refused), declaring `vary: accept-encoding` on every buffered reply while preserving an upstream `vary`, and a `content-length` matching the bytes written. Replies under 1 KiB stay verbatim, encoding runs on the zlib thread pool, and Brotli quality is pinned below its default so network time is not converted into Host event-loop time. Only `application/json` is buffered: event streams and the streaming session-log ZIP export pass through untouched, so any streaming content type stays correct by default. The transport-agnostic Fetch handler is unchanged, so the in-process carrier pays nothing. |
| `dsh-web-app` cold history presentation | Ordinary initial history prefers the latest compact checkpoint transaction and all later events, searching past the message quota by a bounded message budget so a compaction-free session still reads only its tail; older raw history remains reachable through explicit paging, while Chat does not automatically prefetch across a compaction boundary. The click response omits projection restoration, then an idle, generation-fenced projection-only request restores the authoritative baseline after any live-gap repair; a deployment without the projection registry still serves no baseline block. Window-replacing gap repair reuses the same first-screen request. Concurrent command discovery addresses a Session id and applies Agent-scoped shadows only when an Agent is already live, so it never resumes a cold Session merely for a scope key. Model-directory observation preserves a live in-process pick, otherwise reads the latest stored request header through a non-mutating persistence observation that runs beside detached history rather than behind its per-id mutation chain. Batch replay keeps every raw Session Event and Match; Definition-owned `skipHistoryUpdates()` policies may omit only their own superseded State transitions. Chat and Trajectory preserve first-token timing and usage while skipping finalized text reconstruction. Addressed subagents, Definitions without a policy, interrupted streams, live append, ordinary boundary prefetch, paging continuity, and registry rebuild semantics remain unchanged. |
| Source container/Tailscale launcher | `pnpm run web:container` persists a development CA and leaf certificate under `$DSH_HOME`, maps certificate SAN hosts to the same authenticated Web profile's Host trust, optionally carries exact advanced Origins, and leaves browser enrollment and owner approval intact; it is not a separate profile or plugin composition. |
| `auth` profile | `dsh-auth-app` parses device, Grant, API-client, and enrollment-invitation (`dsh auth code issue|list|revoke`) management arguments inside the plugin tree and exits without mounting an Agent, WebServer, or authentication runtime Provider. |
| Notification | No shipped bundle mounts `dsh-notification-http`; explicit examples compose Storage plus the Provider. |
| `dsh-web-app` plugin diagnostics | Mounts the diagnostics registry and Cordis lifecycle Provider before the existing authorized plugin-inventory Remote; the existing Plugins Settings tab displays each current report without repair controls. |
| `dsh-web-app` terminal controller | Mounts `dsh-api-terminal-controller` beside the API gateway: authenticated panels reach the user's own system-user terminals (full harness environment, login+interactive shell startup) through the `terminal/*` gateway Remote endpoints and the `events.terminal` / `events.hold` SSE streams. The controller is a Host Remote — not an Agent tool — so terminal traffic is never model-visible, and the spawned shells are never sandbox-confined. |
| `dsh-base` Agent capability composition | Mounts the generic recipe/member composition registry; global Agent structured overrides inherit into Profile values, while source YAML rows supply native selection, member, and configuration defaults. |
| `dsh-web-app` capability management | Mounts the authorized recipe/member Remote, a Profile Assembly Settings tab, and a read-only Session Capabilities view. Catalog reads parse Profile files and discover Skills without starting a Profile. The next standing generation compiles selection and owner-declared configuration into Loader patches, applies Tool/Skill/MCP member allowlists through native registries, and records resolved members; running Sessions stay pinned, Host-shared MCP connections remain live, compatible read-only Cordis Inspect providers hold shared generation leases, and shared Subagent providers remain read-only. The Session read answers a live Agent from its own generation and a cold listed Session from the standing generation of the Profile its log recorded, starting no agent, session, or turn; a Session persistence does not list stays an error. |
| Opt-in SQLite session persistence | Uses schema 18 packed physical rows for compatible streamed text, reasoning, and tool-call deltas, selective Zstandard payload compression, compact provenance encoding, checkpoint-bound window restoration, packed-range suffix/history reads, and write-locked tail repair. Logical `SessionEvent` semantics and the default JSONL composition remain unchanged; unsupported older SQLite schemas are refused rather than migrated. Invalid checkpoint metadata, prefix hashes, or bounded reads fall back to complete loading. |
| Session checkpoint recovery | `dsh-session-persistence` accepts verified pre-replacement surface state and absolute-sequence suffixes from SQLite, including partial compaction; caller-held complete-history snapshots remain usable after backend closure. Windowed Sessions weakly cache historical payloads and full snapshots so released history is collectible while the Agent remains live. `dsh-session-projection-cache` hydrates registered projection cells at Session creation and treats unusable rows as cache misses. Inbox replay retains complete-history semantics; full-replay peaks, historical index metadata, and physical-prefix hash I/O remain. The [Loader cold-recovery composition](packages/session/session-persistence-sqlite/tests/loader-composition.spec.ts) exercises both whole and partial replacements through Agent resume. |
| `dsh-workspace` storage | Keeps the shared `workspace` domain at official DSH version 2, isolates the Harniverse-only deletion journal in `workspace_deletion` version 1, and explicitly migrates legacy version 3 workspace media. |
| `dsh-tool-todo` continuation | Optionally queues a system-injected continuation at `agent/turn-stopping` while the latest TODO snapshot remains unfinished. The Session surface keeps it as a user-role message for wire compatibility, while its source carries `form: 'system-injection'`; consecutive-turn caps and competing-input suppression remain enforced. |
| Cross-session query and delivery | Separates current-title/creation/raw-activity discovery from content search and exposes exact status, folded messages, complete raw history and windows, and session or event lineage through one `session_inspect` contract; cwd remains an optional filter rather than exact-target authority. The delivery Definition, local Provider, and Consumer expose `session_create(agent_profile_id)`, non-waiting `session_message`, and safe idle-only `session_unload`; ordinary targets use the Agent inbox, while direct subagent targets retain parent authorization and cold recovery through `ctx.subagents.followup()`. Native synchronous and asynchronous delegation results render the child Session id and direct the model to `session_inspect`; asynchronous continuable results additionally direct later turns to `session_message`. The shared base, ACP example baseline, and Web Agent Profiles expose the unified delivery tools beside one continuable spawn-backed `subagent` tool and keep fork/spawn providers mounted as runtime capabilities; the fork alias, global control/list/history tools, and child report tool remain optional plugins outside shipped model rosters. `session.workStatus` folds one admitted message through queued, claimed, settled, and discarded lifecycle states with its effective queue/steer delivery when known, while `session.updateQueue` returns that post-mutation status with the exact message id. Indexed discovery/search opens lazily on first use. |
| `dsh-web-app` Workspace composition | Registers the Workbench navigation and the independent `shell.overlay` preview contribution through the plugin slot system. The shared Workspace store keeps tabs and preview state across placements, while `rightMode`, `rightOpen`, and `rightDrawer` owner facts prevent duplicate surfaces and clear stale preview visibility on right-region changes. |
| Archived Session Web management | Web Archive requires an idle session, closes an idle attached Agent before committing the archive set, makes archived Sessions read-only at Host mutation boundaries, supports durable `workspace.unarchiveSession`, paged read-only browser previews, and single or dependency-ordered batch deletion over the existing journaled `session.delete` transaction. Legacy archived Agents are closed when they reach quiescence; shared attachments remain retained for global garbage collection. |
| Web Agent Profiles | `session.create({ agentProfile })` creates a distinct Agent instance with immutable durable Profile identity. Profile metadata selects the pre-publication permission preset; resume, fork, cold presentation, delivery, child inheritance, and browser summaries preserve the same identity. The four shipped Profiles default to `workspace-write`, and no Profile-switch method remains. |
| Native Child Profile foundation | Native child creation accepts a host-resolved `childProfile`, applies its bounded workspace cwd and Tool allowlist, and persists the profile snapshot in descriptor version 3. Standard, Code, and Cordis expose `child_profile_define` and `child_profile_list`; custom Profile member selection gates the read and write tools independently. The list returns the exact live parent's grant and private revisions, while each child keeps the selected immutable snapshot after that in-memory registry ends. |
| Web Child Profile and history surface | Standard, Code, and Cordis Web Agent presets enable Profile management with host-bound grants and default model routes. Delegation results render the durable child Session id to native models and carry the same identity into Trajectory; `session_message` continues an asynchronous continuable direct child, while `session_inspect` reads either mode's folded messages, raw history, event windows, or lineage without cold-resuming it. The optional `subagent_history` Consumer remains available to custom compositions. |
| Web and Profile supervision | The base bundle mounts the supervision Service. Standard, Code, Cordis, and Minimal Profile metadata may pin `supervised` or `unsupervised` independently of permission presets; the Host applies that value before Agent publication, the Web selector changes the live Session through `/supervision`, and Child Profile delegation captures the effective mode in its durable descriptor. |
| Session deletion with private descendants | `session.delete` keeps ordinary fork deletion leaf-safe while deleting cold `origin: subagent` descendants child-first with a cold parent, refusing any still-live hidden child before mutating persistence. |
| `dsh-base` runtime-context snapshots | The `context-snapshot` row publishes the system-prompt contexts plane as durable user-role snapshots — complete at session start, after compaction, and on section-name-set changes; partial (changed sections only, durable `partial: true`) on in-place updates; a cleared marker when the plane empties — appending at step, request-waterfall, and idle manual-compaction boundaries. `dsh-agent-loop` no longer computes or appends runtime context; `dsh-llm`'s `ContextFormed` snapshot variant and the Web conversation captions distinguish partial records. |
| `dsh-web-app` prompt contexts | `dsh-harness-source` supplies the order-−99 checkout-root context in every Web composition; `surfaceContext` now gates only the `app:web-surface` context and `DSH_WEB_URL`. The DSH downstream/third-party disclaimer moved into the `harness:identity` opener (`HARNESS_IDENTITY`, exported; `includeHarnessIdentity: false` removes both), `dsh-app-boot`'s helper is gone, and the bundle no longer depends on `dsh-app-boot`. |
| Web composer commands seat | The composer tool row's command-menu launcher is the `conversation.input.commands` single session seat, declared by `dsh-client-ui-conversation` and filled by `dsh-client-ui-input-trigger`'s `CommandSeat`: the bar contributes `locked`, its textarea focus keeper, and the click-time synthetic-'/' context, while the entry owns the button, the launcher's expanded state, and the toggle through its own service. The seat renders nothing while the plugin is absent, so a late `ui-input-trigger` activation makes the button appear through the slot system's own lifecycle — no composer-side service probing and no session-provide roster churn. |

### Downstream Commit Ledger

| Commit | Plugin-level effect |
|---|---|
| `8dfc9e9a90` | Desktop activity keeps an authorized observation valid across authentication recovery while unavailability and revocation still invalidate it. Built Host qualification moves to an explicitly built native-desktop e2e lane; source-only coverage no longer assumes an existing Host bundle. Packaging resolves canonical staging aliases before directory creation, rejects output inside its input, and applies executable-mode validation only where the host exposes POSIX bits. Compat: v0 internal observation and qualification corrections; no capability, Session schema, durable preset id or wire-contract change. Verify: `pnpm run test:desktop` (146 source tests, 2 built Host tests and 26 packaging tests passed), strict scoped lint, translation pairing and knip. |
| `f4dfe37d0b` | Desktop Host process entry uses Node's `import.meta.main`, preserving startup when macOS `/var` aliases or directory junctions differ from the module loader's canonical path. A built child launched through a directory alias proves normal authenticated readiness and acknowledged exit; desktop fixtures derive native file URLs and canonical home paths. The PTC service-inspection golden now matches its renamed JSDoc. Compat: v0 internal startup correction; no capability, Session schema, durable preset id or wire-contract change. Verify: focused desktop shell/home tests (36 passed), built owned-process tests (2 passed), and `cordis-inspect-jsdoc` ACP replay (1 passed). |
| `4642c54ab3` | The terminal and browser panels move into the Workspace workbench as contributed sections, the client domain-graph gate is repaired and enforced in CI, and the CDP launch endpoint regex stops matching chunk-split stderr lines. ui-layout declares the `workbench.section.tab`/`workbench.section.panel` list holes (owner share `current`/`select`/`request`) beside `center.view` and carries the selected section in the layout store with `ctx.layout.openWorkbenchSection(section, request?)` (the markdown link router's new seam, replacing `setCenterView('browser', url)`); ui-browser and ui-terminal register one tab and one body each — a body mounts only while its section shows, so page/terminal lifecycles ride section activation — and the sidebar footer triggers, their components, and the shared view stores are deleted while the scheduler keeps its center view. Reachability: the workbench renders sections with or without a resolved Workspace (Files/Changes/Search bodies still need one), blank sessions resolve their Workspace in both the workbench account solve and AppFrame's right-region gate, a composer input-dock chip keeps the workbench reachable while a blank session's hidden header keeps the capsule button off, and tablist-level keyboard roving covers contributed tabs. `scripts/verify-client-domain-graph.ts` is rewritten (relative-specifier resolution with a `src/client/` escape hatch, inline `import('…')` beside `from '…'`, a `< 2` non-contract-directories early exit, 1-based line numbers) and joins both CI static lanes as `client-domain-graph` with run-gates membership coverage; the 24 violations it then found are fixed by moving the shared declarations into `contract/` — runtime's `session-state.ts` (list rows, store shape, catalogs, binding, provide descriptors), the conversation read model (`conversation-snapshot.ts`, `pending.ts`, `context-provenance.ts`), the workspaces list state, and ui-conversation's input contract, block model, turn metrics, chat-stats helpers, and `queueReadFaceOf` — plus co-locating sole-consumer files in `skeleton/`; the slot catalog is regenerated for the new homes. `dsh-api-browser-controller`'s `ENDPOINT_PATTERN` now requires `\r?\n` after the captured URL (the `m`-flagged `$` also matches end-of-input, so a chunk boundary inside the endpoint line truncated the CDP session id and Chrome refused the handshake) with a two-reads regression through the fake browser's new `splitEndpointTail` knob and path-routed WebSocket server. Compat: v0 breaking for the two panels' registration faces only (new section holes replace their `center.view` registrations; the sidebar trigger exports are removed — both pre-release unpublished), additive for the layout store/service (`workbenchSection`/`workbenchSectionRequest`, `openWorkbenchSection`) and the new gate; no session-serialized type, SQLite schema, or model-visible contract change. Verify: pnpm run test:gui; pnpm run typecheck; ./node_modules/.bin/tsx scripts/verify-client-domain-graph.ts; NODE_OPTIONS=--max-old-space-size=1536 ./node_modules/.bin/vitest run packages/api/browser-controller/tests packages/client/ui-layout/tests packages/client/ui-workspace/tests packages/client/ui-browser/tests packages/client/ui-terminal/tests packages/client/runtime/tests packages/client/ui-conversation/tests scripts/run-gates.spec.ts --maxWorkers=1 --no-file-parallelism; DSH_SNAPSHOT=replay pnpm run test:web (the `smoke-real` transport-retry case remains an unresolved local timeout and is excluded from the passing checks above). |
| `a51a4a2801` | W14 redefined from the client sandboxed iframe to a host browser process: new `dsh-api-browser-controller` launches a per-Session host-side Chromium (one-time profile directory, `sandbox: 'chromium' | 'none'`, `ambientEnv: 'scrubbed'`) and drives it over CDP through Node's global WebSocket with zero new dependencies — flat sessions, `Page.startScreencast` JPEG frames with acks, `Input.dispatch*`, navigation history through `Page.getNavigationHistory` — exposing observe verbs (`environment`, `list`) and operate verbs (identity-idempotent `create`, policy-refusing `navigate`, `act`, `input`, `resize`, `close`) through the `browser/*` gateway Remote beside the `events.browser` SSE stream with the four `browser-*` carrier codes in `dsh-host-apiproxy`; `dsh-client-ui-browser` is rewritten as the frame-rendering panel with pointer/wheel/keyboard forwarding and the client `allowedHosts` policy/settings namespace removed; `dsh-client-ui-primitives` gains `MarkdownExternalLinks`/`renderSafeLink` and `dsh-client-ui-conversation` routes markdown links into the panel through `ILayout.setCenterView(id, request?)` (upstream `openExternalLink` parity); `dsh-web-app` mounts `browser-controller` after `terminal-controller`; six web e2e cases over a local `node:http` origin prove the host egress (the Playwright recorder observes no request from the user page), click/keystroke round trips, phone layout, and the `file:` refusal. Compat: v0 additive (one new host package, one new SSE stream face, and four new carrier codes; the client `browser` settings namespace removal is a pre-release unpublished face; no session-serialized type, SQLite schema, or model-visible contract change). Verify: NODE_OPTIONS=--max-old-space-size=1536 ./node_modules/.bin/vitest run packages/api/browser-controller packages/client/ui-browser packages/host/apiproxy --maxWorkers=1 --no-file-parallelism. |
| `9e2c36ec61` | A host with no browser program is now a reported state rather than a dead end, and the reader can move links off the host: `dsh-api-browser-controller` composes its unavailable reason from the probe it actually ran (the configured `executablePath`, else the `browserCandidates` list) and names the remedy, with the same text on `environment`'s `unavailableReason` and the `create` `browser-unavailable` failure, and adds Edge's macOS/Windows install paths beside the Linux `microsoft-edge` name; `dsh-client-ui-browser` renders that host detail under a notice naming both escapes; `dsh-client-ui-primitives` changes `MarkdownExternalLinks.open` to report whether the owner took the destination, and `renderSafeLink` suppresses the anchor only then — a declining owner leaves the ordinary `target="_blank"` behavior inside the same click, which is what opens the link in the reader's own browser without a blocked popup; `dsh-client-ui-conversation` gains the durable `ui-conversation.linkDestination` field (`'panel'` default, `'device'` declining every destination) in its existing settings namespace plus a `settings.general.item` row, and both of its rows share one `PreferenceSelectRow` shell. Compat: v0 breaking for in-repo markdown owners only (`MarkdownExternalLinks.open` returns `boolean`; the type makes a stale implementation a compile error), additive for the durable settings section (one optional field with a default) and the carrier text; no session-serialized type, SQLite schema, or model-visible contract change. Verify: NODE_OPTIONS=--max-old-space-size=1536 ./node_modules/.bin/vitest run packages/api/browser-controller packages/client/ui-browser packages/client/ui-conversation packages/client/ui-primitives --maxWorkers=1 --no-file-parallelism; DSH_SNAPSHOT=replay ./node_modules/.bin/vitest run --config vitest.web.config.ts apps/web/tests/markdown-inline-code-links.e2e.ts apps/web/tests/settings-chrome.e2e.ts. |
| `d58173ce68` | Panel defaults that no deployment could use: `dsh-api-browser-controller` replaces the `sandbox: 'chromium' | 'none'` config with `'auto' | 'chromium' | 'none'` (default `'auto'`) and resolves it against the harness's own privilege in `sandboxDisabled(sandbox, uid)` — Chromium's zygote refuses to start as root without `--no-sandbox`, so the previous default left every root container answering `Remote invocation failed` for every URL — logs the downgrade, and converts every launch/CDP-connection failure into `RemoteError('browser-unavailable')` carrying the browser's own stderr tail (`BrowserLaunchFailure`), replacing the gateway's opaque internal fallback; `dsh-subprocess` gains `SubprocessTerminalSpawnSpec.term` (the terminfo entry published as `TERM`, default `'dumb'`), `dsh-subprocess-local` stops hard-coding node-pty's `name: 'dumb'` — which silently overrode the `TERM` a consumer put in `spec.env`, leaving `clear` writing zero bytes, `tput colors` at -1, and every full-screen program inert — `dsh-ssh`/`dsh-subprocess-ssh` forward `term` through the protocol's `terminal` object, and `dsh-api-terminal-controller` requests `term: 'xterm-256color'` for the human-driven PTYs. The browser-panel e2e lane drops its `sandbox: none` relaxation so the shipped default is what carries the launch in a root container. Compat: v0 additive at the seam (one optional spec field, one widened config union with a new default, one new SSH protocol field); model-facing PTYs keep the capability-free `'dumb'` default byte for byte, so no model-visible contract, session-serialized type, or SQLite schema changes. Verify: NODE_OPTIONS=--max-old-space-size=1536 ./node_modules/.bin/vitest run packages/api/browser-controller packages/api/terminal-controller packages/subprocess/subprocess-local packages/ssh/subprocess-ssh packages/ssh/ssh --maxWorkers=1 --no-file-parallelism; DSH_SNAPSHOT=replay ./node_modules/.bin/vitest run --config vitest.web.config.ts apps/web/tests/browser-panel.e2e.ts apps/web/tests/terminal-panel.e2e.ts. |
| `20a9ef76f9` | Terminal panel defects: `dsh-client-ui-terminal` stops xterm painting over the empty state (placeholder/surface mutual exclusion — a hidden sibling would be measured by the FitAddon to a 1x2 grid), resolves the presentation from the surface's `--dsh-terminal-*` custom properties through `getComputedStyle` (background, foreground, cursor, selection, font family/size with monospace/13px fallbacks), re-resolves on theme revision, refits on visualViewport resize/scroll and `document.fonts.ready`, and adds the nine-key control bar (Esc/Tab/Ctrl C/D/Z and the arrows) whose presses never steal surface focus, plus the `[data-viewport='phone']` form; `ui-terminal`/`ui-browser`/`ui-governor`/`ui-agent-preset` replace six phantom tokens with real `ui-theme` aliases, and `scripts/verify-client-css-tokens.ts` now rejects fallback-bearing `var()` uses (a fallback would ship an actual value the theme owner cannot change). Compat: v0 additive (client-only presentation changes and a stricter repo-local gate; no session-serialized type, SQLite schema, plugin public API face, or wire contract change). Verify: NODE_OPTIONS=--max-old-space-size=1536 ./node_modules/.bin/vitest run packages/client/ui-terminal --maxWorkers=1 --no-file-parallelism. |
| `45a691f127` | Wave-3 fifth batch W13/W14 client panels: `dsh-client-ui-terminal` mounts the terminal center view over the `terminal/*` gateway verbs and the `events.terminal`/`events.hold` streams (exclusive input attachment, read-only followers, bounded slow-follower re-attach ladder, ResizeObserver-driven resize), and `dsh-client-ui-browser` mounts the browser center view with the sandboxed-iframe carrier, bounded app-owned history, and the `allowedHosts` navigation policy through the `browser` settings namespace; the lifecycle goldens record the two new sidebar footer triggers. Compat: v0 additive (two new client packages and slot contributions; no session-serialized type, SQLite schema, plugin public API face, or wire contract change). Verify: NODE_OPTIONS=--max-old-space-size=1536 ./node_modules/.bin/vitest run packages/client/ui-terminal packages/client/ui-browser --maxWorkers=1 --no-file-parallelism. |
| `bad4b0bb1c` | Wave-3 fifth batch W13 host: `dsh-deque` and `dsh-api-terminal-controller` port the W13 host half from the `wave3-continued` reference (61b65a8f5e/0e2256eceb/f8b4650396) adapted to the user-terminal requirements — `ambientEnv: 'full'` with `TERM=xterm-256color` + `DSH_SESSION_ID`, login+interactive default shell startup (never `--noprofile`/`--norc`/`-f`), no sandbox confinement, and no model-visible terminal events. Adds the structural `RemoteError` to `dsh-typert-protocol` with gateway passthrough, the three terminal carrier error codes, the `events.terminal`/`events.hold` SSE transport in `dsh-host-apiproxy` with client fake coverage, and the `dsh-web-app` terminal-controller row. Compat: v0 additive (new host packages and carrier endpoints; the user terminals are system-user surfaces outside the Session log, so no session-serialized type, SQLite schema, or plugin API face changes). Verify: NODE_OPTIONS=--max-old-space-size=1536 ./node_modules/.bin/vitest run packages/util/deque packages/api/terminal-controller packages/host/apiproxy --maxWorkers=1 --no-file-parallelism. |
| `661a41b184` | Wave-3 fifth batch ambient environment: `dsh-subprocess` specs gain `ambientEnv?: 'full' | 'scrubbed'` — `'scrubbed'` (default, previous behavior) keeps the credential-and-`DSH_*`-scrubbed parent base, `'full'` inherits the harness `process.env` verbatim; `dsh-bash-local`/`dsh-pwsh-local` stamp `'full'` under `danger-full-access`, the W13 user terminal spawns `'full'`, and the model PTY keeps the scrub. Also allows the ssh helper-runtime handshake's literal `cordis` profile id in the rescope gate (a master gap: the id is product data). Compat: v0 additive (an optional spec field with the previous behavior as default; confined spawns and model-visible processes are byte-identical). Verify: NODE_OPTIONS=--max-old-space-size=1536 ./node_modules/.bin/vitest run packages/subprocess/subprocess-local packages/shell/bash-sandbox packages/shell/pwsh-local --maxWorkers=1 --no-file-parallelism. |
| `be5ea3a9c055ff5e9de409c972e96fd3dece84c9` | Adds the `dsh-client-authentication` Service Definition and browser Provider, adopting one bootstrap runtime for proactive renewal, bounded classified-401 recovery, cancellation, and terminal refresh instructions. Connection, module loading, and HMR consume that lifecycle; `dsh-client-ui-connection-status` fills the sidebar-owned read-only status seat through declaration injection and framework hooks. HTTP uplinks, downlink-only WebSockets, Host credential bounds, and authorization remain unchanged. |
| `99e51e45e4` | Moves the Web composer's command-menu launcher into the `conversation.input.commands` single session seat (plan/model seat pattern): `dsh-client-ui-conversation` declares the seat and contributes `locked`, its textarea focus keeper, and a click-time synthetic-'/' context (`CommandToggleContext`), while `dsh-client-ui-input-trigger`'s `CommandSeat` owns the plus button, the launcher-driven expanded state, and the toggle through its own session controller. Presence replaces the stranded optional-service probe (the cold-start disabled button), the recovered-surface e2e golden records the enabled launcher, and an interim provide-roster existence-seat approach was reverted after CI showed runtime roster churn re-running every cached session inject. |
| `0e7601eb16 (client)` | A5 generic file upload, client half: `dsh-client-connection` XHR upload transport (progress, AbortSignal bridge) against `POST /api/attachment/upload`, `dsh-client-ui-attachment` `FileChips` rail (pending/error/remove states in the B7 link-color language), `dsh-client-ui-conversation` composer file entry with file-part assembly on submit and `user/file` badge rows on the owning user message; keyless e2e `apps/web/tests/file-upload.e2e.ts` covers pick → route → admission → event pair → badge DOM → cross-origin fence denial. |
| `0e7601eb16 (admission)` | A5 generic file upload, admission half: `durablePromptContent` becomes a `{blocks, files}` transaction (`readFile` verification + `publishFileHandle` publication + deterministic Chinese handle-text text blocks; any failure rejects the whole prompt), new log-only `user/file` session event appended immediately before its `user/message` (position-correlated), and `MessageSource.user.files` carrying deduped refs; persistence catalog regenerated in-commit (`KNOWN_SESSION_EVENT_TYPES` is generated). |
| `0e7601eb16 (route)` | A5 generic file upload, route half: `POST /api/attachment/upload` in the `dsh-client-connection` Host face — trust fence → authentication → `harniverse.operate` gate (observers denied through the closed four-capability vocabulary, no fifth capability minted) → store capability probe → Content-Length precheck → chunked capped body → receipt JSON; raw-bytes body with URI-encoded `x-attachment-name`. |
| `0e7601eb16 (storage)` | A5 generic file upload, storage half: `dsh-attachment` seam File vocabulary (`FileAttachmentRef`/`SaveFileAttachment`/`StoredFileAttachment`/`FileAttachmentLimits`) with default-denying `saveFile`/`readFile`/`publishFileHandle` store members; `dsh-attachment-local` content-addressed files (`saveFileObject` verbatim bytes, `readFileObject` digest re-verification, `publishFileHandle` idempotent 0o444 hard links under global `attachments/v1/links/`), `maxFileBytes` default 100 MiB, and the shared `publishObject` publication skeleton extracted from the image path. |
| `ebf2a884a1` | Ports the official B7 polish trio (`6d6f8f044c`/`9ecd18e986`/`75d3402b84`) adapted to our tokens: `ui-theme.fontSize` setting (tiers 14/16/18, default 16) with the `--dsw-content-font-size/-delta` axis through the markdown ladder and flow furniture (style-contract pinned; secondary tiers and app chrome fixed), `ConversationRoot` adaptive content width (`clamp(748px, 64%, 920px)` over a ResizeObserver-published column width; no drag chrome), and slash-menu bare-Tab completion beside Enter (skill badges audited render-time stable, pinned). |
| `d8a3d3ce86` | Ports the official clickable-link language (`8ffdee4fe5`) into the client: dedicated `--dsw-alias-link` token (light deepseek-500 / dark deepseek-400) decoupled from `state-business-primary`, `ui-primitives` `LinkIcon` category glyphs leading markdown anchors / file mentions / URL-promoted code / web links / produced chips, weight-500 + dotted 3px-offset hover/focus underline across those surfaces plus workflow member labels, subagentLink token re-binding, and a style-contract spec pinning the link token and secondary font tiers. |
| `2ae6d26ab6` | Wave-2 A2 harvest for `dsh-session-persistence-jsonl`: torn zstd frames now recover at complete-block granularity through a private-handle streaming prefix decoder (drained plaintext survives decoder errors); the torn marker carries `{ truncateTo, recovered }` (coordinator-opaque) and `commitRepair` rewrites recovered rows as their own fsynced frame before closers. Five other harvest items audited as already present; two error classes deferred for lack of a consumer. |
| `5f03471659` | Ports the official Safari textarea soft-wrap shrink recovery (`4edbdd443e`) into `dsh-client-ui-conversation`: Safari-only pre-paint one-pixel reflow restoring the one-scrollport zero-overflow invariant after native draft shortening; non-Safari browsers read no geometry (guard-pinned). |
| `c6372de657` | Declares the session format v0 edge (wave-2 A1): `classifySessionFormatVersion` joins the `dsh-session-persistence` seam as a total four-value classification face (`current`/`migration-required`/`unsupported`/`malformed`) over stored header versions, additive to the existing stamped-version and fail-closed refusal mechanics of both backends. |
| `c30e963879` | Closes the wave-2 D1 Host-header authority audit with no plugin change: `isTrustedApiRequest` is confirmed as the single deny-only fence (loopback or declared `trustedHosts`, fail-closed ahead of authentication; loopback bypass keyed to the listener bind), and a new pinning test proves a forged `Host` claiming a declared authority still authenticates nothing (401). |
| `bf1ec50830` | Absorbs the eleven remaining subprocess/PTY observation and settlement members (`32ddfcd89c`, `9757224349`, `9a12505f86`, `5467685bc1`, `2338f4ad14`, `33e90e9919`, `5375142827`, `60587b4901`, `03186fe93f`, `a163f4019b`, `f1fd304dff`): one `ProcessSnapshot` per poll round with signal-time re-reads, waiting-thread controlling-terminal matching with an emulated-ABI syscall family, lazy Windows tree enumeration, and memoized Win32 ACL settlement (terminate on first drain failure, abort siblings, aggregate closure and termination failures, `finally` handle closure). |
| `bf1ec50830` | Hardens the Python CodeRuntime (`8e9d5467b0`, `2df28fd249`, `bca392e6d1`, `d9ed44d62c`, `d6bd5eb973`, `7f84a825c9`): bounded reply and in-flight binding backlogs (1024 each), one-read binding metadata snapshots against hostile getters, and load-time CPython resolution plus a `-I` version probe requiring CPython ≥ 3.10; the hold-seal member is an evidenced no-op (no fragmented log arrays in this runtime). |
| `117d04ff52` | Absorbs the official outbound proxy policy (`c62d6f3a44`, `8470ddef1d`, `545e2ad914`) natively without the `undici` dependency: new `dsh-http-proxy` library resolves one policy from the launch-environment snapshot and installs a dispatcher behind the symbol global `fetch` resolves (per-request absolute-form/CONNECT tunnels, delegate passthrough for direct URLs, `NODE_USE_ENV_PROXY` child overlay with user-value preservation and flag withholding); `dsh` profile boot installs/disposes it; `dsh-web-fetch-http` routes proxied URLs through the shared tunnel with no local DNS pinning while loopback/bypassed URLs keep the pinned direct transport; `dsh-subprocess` overlays the child proxy environment in `scrubbedParentEnv`; llm-pi-ai is covered through global `fetch` with egress specs through a fake loopback proxy. |
| `117d04ff52` | Adapts official `c58097a826`: the JSONL backend takes a kernel-arbitrated cross-process write lease per session directory (POSIX `flock` through a direct libc koffi binding; Win32 named semaphore derived from the lock path) from the session's first durable write, rejects contending appends, repairs, and deletion with `SessionAlreadyOwnedError` while another process holds the lease, releases through a new optional `PersistenceBackend.releaseWriteOwnership` hook on retirement, deletion, or dispose, and aggregates release failures at teardown; torn-tail recovery runs unchanged under the successor's lease, and the arbitration primitives are injectable for cross-platform tests. |
| `117d04ff52` | Extracts the client store engine into `@deepseek-ai/dsh-client-store` with contract types and a pure ui-slots re-export shim; runtime consumers and package manifests point at the new engine package while the mechanical move preserves the existing store API and test behavior. |
| `f4bc0da7ee` | Absorbs four official areas: the one-shot spill startup sweep (`cleanupPeriodDays`, exact-shape best-effort deletion with write-side retry), streamed tool-call identity semantics in the DeepSeek adapter (empty/null re-sends no longer clear identity; the upstream-reverted finish guard intentionally not ported), Object.is view-gated projection change feed (no client push for internal-only state), and Anthropic Messages native discovery gated on explicit protocol declaration (draft or stored profile). |
| `3844a2c64c` | Absorbs four official areas: optional webserver gzip (composition-level opt-in, q-value negotiation, shipped web bundle enabled), duplicate-install-safe Typert Remote prototype descriptors, fully-qualified workspace path canon with root-aware default titles plus Windows-correct client joins, and model-facing fs fixes (extension-less `read_image` sniffing, unified unread-mutation diagnostics); session-surface orderings and absence-after-reread verified already guarded and pinned by tests. |
| `4c31133a0a` | Absorbs three official Windows fixes at contract level: true two-byte-NUL UTF-16 termination in the native directory picker, `windowsHide` on non-terminal subprocess spawns and both taskkill helpers, and bounded transient-rename retry inside `writeFileAtomic` (Windows-only; POSIX unchanged). |
| `de672a15e2` | Generates the committed API catalog (`docs/api-catalog.json`) from the compile-locked unary/carrier/error registries, wires `verify-api-catalog` into doc-sync, locks `RPC_ERROR_CODES` to the error-details map, and replaces the host `FrameQueue` and client WebSocket inbox `Array.shift` backing with ring buffers. |
| `ae7c9b6d5b` | Makes the shipped model-provider composition vendor-neutral and removes the DeepSeek-specific onboarding occupant. |
| `e054be808c` | Adds explicit Agent/Session close, cold deletion, resumable event cursors, projection-cache deletion, and workspace cleanup across existing plugins. |
| `27f702fac1` | Adds explicit all-interface Web startup acknowledgement; later superseded by authentication and TLS requirements. |
| `313a859200` | Mounts configurable DeepSeek/Exa/Perplexity search providers and adds live settings/UI selection. |
| `fd707f8b78` | Disables the model-facing native Web tool surface in shipped defaults. |
| `a9fab6b33b` | Adds the outbound notification Definition/coordinator and durable HTTP Provider. |
| `7b11e67e00` | Adds durable final-result artifacts, `artifact_read`, bounded readers/search, stronger compaction evidence, and accurate auxiliary usage. |
| `5dde5cdc6f` | Adds inbound authentication Definition/Provider, Web transport enforcement, browser login, and token management. |
| `c44c47bbba` | Makes the read-result UI accept bounded pages without an exact total line count. |
| `795f93ba84` | Requires TLS for non-loopback Web serving, adds secure cookies and peer-aware auth failure limiting, and fixes auth lock teardown. |
| `6ba41ae8ef` | Replaces named-token inbound authentication with plugin-native public-key Enrollment/Grant/Access lifecycle, capability-total endpoint enforcement, bounded enrollment and per-Grant credentials, browser/API-client flows, and sealed owner admission. |
| `22c9c1b9a9` | Adds automatic lossless-style compaction over a committed summary DAG, bounded current-session history tools, and shipped bundle, preset, and headless composition. |
| `827dc083d5` | Adds the read-only plugin diagnostics Definition, Cordis Provider, authorized Remote projection, and Plugins Settings report. |
| `955ab7323a` | Adds the source container TLS launcher, Tailscale Host/SAN wiring, exact Origin trust configuration, human-readable enrollment errors, and privacy-minimal Web connection and authentication diagnostics. |
| `5158dec7a0` | Keeps the shared workspace domain compatible with official DSH, isolates Harniverse deletion recovery state, and migrates legacy workspace media explicitly. |
| `5816aa6d30` | Adds optional TODO-driven continuation turns with a bounded consecutive-turn policy and competing-input suppression. |
| `093c319309` | Adds cross-session ID-bound query, runtime status and message tails, ordinary-session message delivery with cold resume, and safe idle-only unload. |
| `baafb40c0d` | Makes Agent Profile identity immutable at Session creation, applies Profile default permissions before publication, and removes blank-Session Profile switching. |
| `7e38080de9` | Mounts cross-session query and delivery in shipped compositions, with Profile-scoped Web Consumers and lazy first-search SQLite activation. |
| `b2016ed5f2` | Adds current-title, creation-time, and raw-activity session discovery plus complete raw-log tail and window reads while preserving content-search hit semantics. |
| `995b34867c` | Adds inherited Agent Profile capability composition, static recipe planning, immutable Session generations, Host-shared MCP restrictions, and Web management/read-only runtime views. |
| `1dfeaaaea5` | Adds Profile-level Tool, Skill, and MCP member allowlists, owner-declared Persona configuration, native registry enforcement, and immutable generation member projections in the Web UI. |
| `44956e699b` | Batches concurrent authentication admissions and registers initial browser plugin factories through one revision-addressed aggregate while preserving independent fallback and HMR. |
| `8719eabfa4` | Adds direct model-requested retained-tail context compaction through the existing Provider transaction and lossless summary DAG. |
| `dcd0413ffa` | Splits browser plugin startup into authenticated critical/deferred bootstrap phases, adds a lightweight auth entry, Brotli/immutable static asset delivery, Web-profile cold-summary probe suppression, and restored-history/baseline overlap without weakening plugin-route authorization. |
| `868883546d` | Adds provider-local bounded DeepSeek request-image projection, model-level explicit image modality opt-in, Files API upload reuse with scoped owner-only index, quota cleanup, stale-file inline retry, and all-inline fallback. Default catalogs and shipped Profiles remain text-only; no durable Session schema changes. |
| `2157a1d84f` | Replaces scalar SQLite session-event rows with schema-17 batch-local packed rows, selective Zstandard payloads, compact provenance, bounded packed-range reads, and stale-safe physical-tail repair while preserving logical persistence semantics. |
| `f0807f6c08` | Serves ordinary initial session history from the latest compact checkpoint transaction and later events under a bounded checkpoint-search budget, moves projection restoration off the click path onto an idle generation-fenced request, threads AbortSignal into persistence reads, and keeps Chat from automatically paging across a compaction boundary. Batch replay preserves every raw Session Event and Match; only Definition-owned superseded State transitions are omitted. |
| `87ffb7eb28` | Negotiates `content-encoding` for buffered Host `/api` replies (Brotli, else gzip, else verbatim; `q=0` refused) on the zlib thread pool with Brotli quality pinned below its default, buffering only `application/json` so event streams and the streaming session-log ZIP export keep their incremental delivery. The transport-agnostic Fetch handler is unchanged. |
| `f4b03623f6` | Keeps global capability defaults separate from Profile-native rows, exposes Code Mode's `run_code` as a selectable member, and proves Standard remains native while Code remains `run_code`-only through the real Web composition. |
| `57db49dcf0` | Keeps cold-session command discovery and model-directory observation off Agent resume: discovery applies scoped shadows only for an already-live Agent, while detached model selection reads a validated stored request-header prefix beside history rather than behind its per-id mutation chain, preserving publication, subagent, close, delete, format, and disposal fences. |
| `d5caac49c1` | Ports official `82db1515fb`: every bwrap profile uses a private PID namespace and matching procfs, while the functional probe uses the same profile so unsupported hosts fall through the existing fail-closed Linux ladder instead of accepting weaker confinement. |
| `000275810e` | Ports official `583894f7ae`: the DeepSeek adapter replays exact reasoning content on every reasoned assistant turn, including plain answers, so compatible gateways can recover upstream thinking signatures. |
| `40882c1c4f` | Adapts official `92723cafeb` and `600f3a3110`: the authenticated Web composition declares `/auth/manage` as an exact shell entry while missing assets and undeclared paths return empty 404 responses. |
| `b3db1057f5` | Ports official `47399764c5` and `7b973e27c8`: publication ordering preserves installed edges and best-effort peer edges, while Host and Client source gates reject module-scope loads of optional dependencies. |
| `444d9f3326`, `cb84b86324`, `35d28a024a` | Adapts official `93cbb3799d`, `66a7081c15`, and `738dcced9b`: the `harniverse` build profile titles the shipped browser application `Harniverse`, feeds identical public `DSH_CLIENT_*` values to the Web shell and every dynamic client bundle, and binds one complete build to its artifacts so `release:pack --family dsh` refuses a missing, foreign, or stale client build record. Authentication and authorization remain runtime plugin decisions. |
| `38e82f9667` | Adapts official large-history pagination hardening: shared, JSONL, Zstandard, and SQLite history reads scan provenance minima iteratively, so a finalized message with an unusually large `sourceEventSeqs` list cannot overflow the JavaScript argument stack. |
| `f4023b90a8` | Adapts official composer transaction and cache-display fixes: one cancellable operation owns reference preparation and send settlement, unresolved drafts and image reservations cannot be submitted twice or erase later input, and near-complete cache hits remain distinguishable from an exact 100 percent hit. |
| `86f340a1ea` | Adapts official bounded multi-query Web search: one model call accepts a bounded `queries` array, runs scalar providers concurrently with sibling cancellation and quiescence, deduplicates queries and URLs, merges sources round-robin under one cap, and records query-labelled durable output. |
| `034ea62b3f` | Adapts the official shared Settings description mirror to Harniverse ownership: one generation-fenced read feeds all Settings Consumers, Host topology events invalidate exposed descriptions, and unary plus stream transport identities prevent stale or cross-principal snapshots, writes, and secret-bearing model discovery from crossing authentication generations. |
| `614a79d862` | Keeps authenticated browser transport values inside the inline-safe API wire layer: connection and Fetch carriers import only authentication types, principal identity schemas retain wire validation without runtime branding, the source purity regression scans client and inline-safe carrier edges, and build failures identify the importing module. |
| `ba26e84caf` | Makes the public unit-test workflow build the complete artifact tree before Vitest, keeps `check:all` on one dependency-ordered build, and lets third-party notice generation ignore pnpm virtual-store entries whose platform-optional package was not materialized. |
| `132d22be25` | Records build-before-unit verification as a repository-level requirement and removes obsolete host-agent sandbox escalation guidance without changing the product sandbox's plugin-owned confinement and approval rules. |
| `af1ef802d0` | Adds idle-only Archived Session Web management, read-only archive previews, unarchive, and dependency-ordered deletion. |
| `7a9f18df5e` | Carries the admitted identity in every connection `server-response` envelope so authenticated browser Remotes validate, and reads a cold listed Session's capability assembly from the standing generation of its recorded Profile without starting an agent, session, or turn. |
| `6ef9ea1bc1` | Makes the Archived Sessions entry reachable from the collapsed sidebar rail and expands the rail before opening the archive panel. |
| `2c925a4f50` | Reserves the full width of the three desktop sidebar header actions so the archive control is not clipped or covered by the adjacent conversation column. |
| `f7c1bc1308` | Refuses a composition plan whose selected recipes claim one member name, and compiles an already-stored tool-name conflict into a mountable generation so a Profile carrying one can still start a Session. |
| `02ff1b54f8` | Keeps the vendored logger-console timestamp visible on dark terminals by using ANSI palette index 7 instead of dark index 8; log thresholds and message content remain unchanged. |
| `ac8d0ffdfa` | Recovers unusable pi-ai replay metadata as provider-neutral history, aligns replay envelopes with max-token block pruning, and persists visible text/reasoning prefixes from cancelled streams as interrupted assistant messages for replay and UI projections. |
| `44ac9c5d23` | Adds bounded provider-authored diagnostics to one-shot subagent results, preserves safe Claude Code failure and unattended-interaction facts, classifies Codex protocol/process/permission failures without retaining raw payloads, and presents diagnostics separately from partial assistant output. |
| `60fd18012c` | Adds capability-gated Host file-reference discovery, bounded local Agent-workspace indexing, authenticated Session-reference candidates and pre-step preparation, and the unified Web `@` completion with quoted paths, directories, and canonical Session mentions. |
| `fa35fa4edc` | Adds the opt-in Python `CodeRuntime` provider: fresh shell-free CPython subprocesses, hostile fd-3 JSONL validation, async bindings, resource/time/output limits, lossless JSON, cancellation, and quiescent disposal without changing shipped Profiles. |
| `18fe061761` | Adds creation-time-fenced Windows process inspection and PTY teardown plus the opt-in owner-scoped persistent PowerShell tool, preserving existing POSIX behavior and leaving default Profile composition unchanged. |
| `644e92524d` | Carries typed image attachments through slash-command adjudication, transport, and Host admission; commands must opt into images, handlers receive durable `ImageBlock` references, and failed or cancelled submissions retain draft resources. |
| `64ed2f1efc` | Aligns the accepted upstream feature contracts in regression fixtures: Web reference rows insert into the actual Web layer, interrupted output remains visible after cancellation, command image arguments are explicit, and provider diagnostics are asserted separately from failure output. |
| `8926dce6ff` | Classifies the new `fileReferences` capability seam in generated service and event graphs, covering its Host implementation and Web consumer. |
| `113785ea1f` | Synchronizes the bilingual LLM streaming and Session type-equivalence contracts for replay envelopes, interrupted blocks, and cancellation-finalized assistant messages. |
| `b7f4473eb4` | Keeps the Client TypeScript face isolated from Host `Context.sessions`: Client project references no longer pull Host-only projects, the API contract barrel leaves runtime schemas on their dedicated subpaths, and Session-reference type declarations use Client-safe `/types` entries. This restores the full Host/Client/Web build without widening the Client program into the Host service graph. |
| `f7131338e2` | Declares `zod` as a runtime dependency of Session-reference so generated Host and Remote Typert artifacts load through their public exports in a fresh install, with regression coverage for both exports and the real Web startup path. |
| `b9524bb737` | Moves Session-reference public `typert` and `remote` export coverage into the post-build built-bin smoke gate, preventing clean-build TypeScript ordering failures while proving both subpaths through plain Node; the repository rule now requires clean-build evidence for build/export changes. |
| `6e5b88ca23` | Adds the parent-private Child Profile foundation: immutable resolved profile types, grant intersection and workspace boundary validation, native Tool/workspace application, and descriptor version 3 persistence. |
| `966538a150` | Ensures omitted Child Profile depth/token limits inherit the parent's granted ceilings instead of becoming unbounded. |
| `db0274419c` | Adds the parent-private Profile registry and revisioning, model-facing define/list plus `profile_id` delegation, opaque primary model-route resolution, native Skill/MCP setup enforcement, and SDK initialize-handshake propagation. |
| `2723f9fe58` | Hides `origin: subagent` sessions from ordinary Host list/search summaries and Host session increments while preserving the parent-owned subagent catalog and descendant APIs. |
| `230a59d2d9` | Makes hidden subagent descendants deletable with their cold parent while preserving leaf-only deletion for ordinary fork descendants. |
| `8810402740` | Adds Web Trajectory child-history navigation and native raw-event paging for authorized `subagent_history` reads, with SQLite/JSONL bounded readers and documented third-party fallback behavior. |
| `1b0ddfe6a8` | Adds the service-owned unified `sync`/`async` Invocation contract, routes tool-owned foreground and continuable paths through it, makes Agent teardown tolerate best-effort final flush failures, and corrects the cold-resume replacement regression fixture. |
| `70516f51b5` | Adds the model-facing `session_inspect` Consumer with unified summary, message, raw-history, event-window, and lineage views, and updates shipped composition, generated catalog, and bilingual package documentation. |
| `4904050e1c` | Adds the unified `session_message` naming alias for ordinary-session delivery, mounts it through Agent Profile composition, and updates generated catalog and bilingual package documentation. |
| `71ebc17a96` | Changes the subagent presentation metadata to expose caller wait mode as `sync` or `async` instead of provider lifecycle terminology. |
| `946c9f7e24` | Adds the model-facing `mode: sync|async` delegation input and validates async availability through the existing deployment policy. |
| `4b4c4bf5be` | Refreshes the generated model-facing tool catalog for the invocation-mode schema. |
| `64d85fe2f9` | Removes the legacy model-facing `run_in_background` field from subagent delegation; `mode: sync|async` is now the sole model scheduling contract while deployment defaults remain internal. |
| `19b40e2c7b` | Adds `session_create` through the existing Session Delivery Definition/Provider/Consumer seam; creation uses `ctx.agents.create`, resolves the deployment model, mounts the requested Profile before publication, and returns the durable session identity. |
| `1cd0fb8b59` | Normalizes sync and persistent async `subagent` results to Invocation receipts carrying `mode`, `invocationId`, and `sessionId`; the legacy receipt remains only for the internal one-shot Task route. |
| `47b1dc525f` | Changes the shipped fork delegation tool to the persistent Invocation lifecycle, removing the last shipped `backgroundMode: one-shot` composition. |
| `270fa008e0` | Unifies sync and async subagent waits on the durable continuable Session lifecycle, removes obsolete continuable-mode composition flags, and retains explicitly configured one-shot behavior as deprecated legacy compatibility. |
| `e5dde45a8d` | Narrows the async Invocation receipt regression fixture through its public JSON boundary before reading the durable child Session id, restoring strict Host compilation. |
| `60b37766c2` | Restores parseable shipped fork composition and lets private Agent-preset compaction history attach from the first published Session event when its realm did not observe the creation edge. |
| `694662e3a3` | Consolidates shipped Session reads under `session_inspect`, keeps only `session_create`, `session_message`, and `session_unload` for Session mutation, routes direct-child messages through the persistent Subagent runtime, and reduces shipped delegation to one spawn-backed `subagent` tool while retaining fork/spawn providers and optional control/report plugins. |
| `e3ceb12389` | Adds session-indexed LLM wire-attempt diagnostics: adapters record compact request fingerprints, transport status, diagnostic headers, timing, and normalized failures without duplicating Session conversation content. |
| `64a19c1f41` | Publishes adapter-resolved model output capacity separately from request defaults and makes compaction summary calls combine matching Provider usage with model-aware output and remaining-context bounds. |
| `06b0506f77` | Renews browser sessions through repeated device-possession exchange with half-life scheduling, bounded retry, lifecycle wakeups, quiescent logout, and renewable-key admission for ordinary authenticated pages. |
| `6fdf45691a` | Adds the plugin-native Workspace navigation/preview redesign: top-level Files/Changes/Search tabs, Host-compiled include/exclude glob filters, shared docked/drawer preview state, `shell.overlay` composition, sidebar-safe geometry, focus lifecycle, semantic-token enforcement, and authenticated browser ARIA/interaction coverage. |
| `7db5712690` | Adds the independent per-session supervision mode plugin, Profile and Child Profile selection, human-dependent operation enforcement, plan-mode policy, Web selector, persistence catalog entry, assembled Web replay coverage, and bilingual package/architecture documentation. |
| `aa98bbf729` | Completes supervision's deploy-root runtime closure, config and service catalog contracts, public JSDoc, subsystem documentation, and regression-safe Child Profile supervision lookup. |
| `094f2004af` | Adds the provider-neutral WebRuntime registry and explicit search/fetch selection, Tavily/Brave/Kagi/Firecrawl providers, Firecrawl Search/Scrape mapping, HTTP fetch defaults, Web Settings provider controls, and model-facing untrusted Web-content boundaries. |
| `03cfedff74` | Admits each new Web provider's settings namespace to the api-proxy configuration allowlist, so the Settings card serves live Tavily/Brave/Kagi/Firecrawl forms instead of the unavailable notice, and realigns the provider-selection, tool-schema, and untrusted-artifact regression fixtures with the shipped contracts. |
| `1a0d968c4d` | Ships the four Profile capability defaults: Minimal's hidden lossless recovery, Standard-family user Skills/Hooks/MCP loading and disclosure, settings-owned scoped MCP consumers, dialect-specific Hook discovery, hardened public-only Web fetch, and corresponding real-composition, generated-catalog, and bilingual documentation updates. |
| `552a78fbc5` | Unifies all built-in Profile prompt assembly around static Harniverse identity plus dynamic persona, checkout, and Web contexts; disables shipped Claude Code/Codex Hook bridges by default, rejects CLAUDE instruction candidates, and marks automatic TODO continuation with the durable `system-injection` form. |
| `e55e384ae3` | Aligns the moved checkout, Web GUI, and Cordis persona wording with Harniverse branding, identifies Harniverse as a downstream of DeepSeek Harness (DSH), and adds the independent-product and retained-DSH-license notice to the checkout context. |
| `48bdd0860fd5123f6ceb852f3a68c8e0d09c1939` | Exposes the authenticated Web API through the private-profile composition used by the Cordis Web example. |
| `4e5467e4ccf983f4021dff8f7ae061e14865db5a` | Accepts the stock macOS `/var` and `/tmp` aliases only when their real paths remain under `/private`, while retaining ownership, permission, directory, and containment checks for spill roots. |
| `58957069a3d70090b98927b9aa4bf90937af2123` | Restores the validation baseline across CI, vendored plugin rescoping, built artifacts, snapshot fixtures, authenticated Web runtime behavior, and principal-bound hidden-subagent projections while preserving stable product and protocol identifiers. |
| `855388feaf307236cc42271f73394e08536f2ea9` | Treats Windows `EPERM` from an existing authentication lease directory as ordinary contention, preserving the single network-instance contract across platforms; the accompanying fixture-only CI repairs do not alter shipped plugin composition. |
| `98bb7b0a3c2546e47b92afa19a01a29951ea7a77` | Splits the opt-in CPython runtime control transport between stdin for Host frames and fd 3 for child frames, preserving stdout/stderr log capture while making the fresh-process provider portable to native Windows; the accompanying snapshot and platform-fixture repairs do not alter shipped composition. |
| `41e52f248d5465f412643f10e265b95f40ce62b2` | Treats Windows `EPERM` from an existing Grant-registry lock directory as ordinary writer contention and gives the opt-in CPython reply-reader thread enough Windows stack for deeply nested lossless JSON; the accompanying snapshot and test-fixture repairs do not alter shipped composition. |
| `c9a2b0579b7ed9cca63cec4e625e7d16a25581ee` | Prioritizes authenticated Workspace Git cancellation over a racing successful process exit and raises the opt-in CPython reply-reader stack reserve for deeply nested lossless JSON on native Windows; the accompanying cross-platform test-fixture repairs do not alter shipped composition. |
| `145d486c5e` | Raises the opt-in CPython native-Windows reply-reader stack reserve again for the observed 3,000-level JSON boundary and hardens real Loader-composition timing/scrollback budgets for slow Windows CI; these test-fixture changes do not alter shipped composition. |
| `bcc4186fb5` | Resolves the opt-in CPython executable through the Host PATH before clearing the model process environment, and keeps authenticated Workspace directory and Git inspection portable by reserving descriptor-path traversal for Linux while revalidating canonical paths on macOS and Windows. |
| `71befd15fe` | Applies the opt-in CPython address-space bound only where the platform measures address space, skipping it on macOS where `RLIMIT_AS` aliases resident memory, and tolerates individually refused resource requests. CPU and Host wall-clock ceilings remain enforced on every platform. |
| `277be8ca8a` | Treats Windows `EPERM` from removing a vacated inbound-authentication lease as ordinary cleanup contention, preserving the single network-instance contract across platforms, and bounds lease acquisition so an unremovable lease fails loudly. |
| `1e28b9804c` | Splits the scheduler's model-facing tools out of the service into the preset-scoped `dsh-tool-scheduler` plugin, mirroring `dsh-tool-goal`: the service registers no tools, the `standard`/`code`/`cordis` preset rows select the tool plugin, and `minimal` keeps its two-tool contract. |
| `b94a67823f` | Adds the scheduler's session-scoped Typert Remote (`list`/`create`/`update` plus a removal verb, each behind a `harniverse.*` capability) through `dsh-api-remotes`, and the new `dsh-client-ui-scheduler` session-header schedule list with pause/resume/delete, enabled by `dsh-web-app` after `ui-jobs` and rendering only when the session owns schedules. |
| `b2cabd2ae9` | Renames the scheduler Remote's removal verb to `delete`: the generated namespace service rejected `remove` because it shadows a `Service` prototype member, so the wire verb, client projection, header UI, and docs move together. |
| `abed1dfbcf` | Lets `SurfaceManager` fold a windowed log whose first replacement predates the window, so a post-reset cold open can derive the model-facing surface from a checkpoint window while the durable log stays complete; equivalence with a full fold is pinned by spec. |
| `05d86b7c02` | Teaches `Session.fromRestore` to adopt a windowed seed (restore mode only), threading the window base through contiguity validation, `append`, `get seq()`, and the derived-message lookup so absolute seqs stay durable identifiers. Also removes a client-graph type break: the scheduler's public types imported the `dsh-session` package root, leaking Host merges into Client lint programs. |
| `3f5543c13e` | Moves the `dsh-context-reset` and `dsh-scheduler` invariants from the observe-only `session/event` channel to `internal/dispatch`, the shape `dsh-goal` and `dsh-compaction` already use. Contained listener throws meant neither companion could reject a violating append; both now fail at the `session.append()` call site, and their specs assert through real contexts instead of hand-invoking the captured listener. |
| `1642eecaae` | Completes the scheduler's B2 human surface: the client registers a settings management section that aggregates current-workspace session ownership and provides create/edit/pause/resume/delete plus recent runs; the central scheduler records server-derived prompt revisions and durable per-attempt run history, exposed through the capability-gated `runs` Remote. |
| `f03d573d91` | Extends the scheduler host with named-session delivery targets (`target.kind === 'session'`), rule edits through `update` (recomputed next due, finished schedules refuse rescheduling), and the host-authority management Remote (`listAll`/`runsOf` observe, `updateAny`/`deleteAny` operate); prompt edits through the global surface attribute their revision to the record origin. |
| `108c1bcbca` | Adds the preset-scoped `schedule_update` tool: ownership-scoped prompt replacement and pause/resume, with rule and target rebinding deliberately left to the human management view. |
| `bdbde30798` | Adds the `center.view` root list slot to `dsh-client-ui-layout`: `ctx.layout.setCenterView`/`clearCenterView` name one registered full-center view over the mounted (inert) conversation, and a session switch clears it; ui-sidebar stacks multiple footer actions vertically and ui-primitives gains the clock glyph. |
| `949c69b901` | Replaces the settings-section management UI with the global schedules center view: a sidebar footer trigger above Settings, the every-schedule table with a create/edit drawer (bound session incl. any named session, rule, context, status, run history with target session ids), and pause/resume/delete over the global Remote; a shared viewing store ties the trigger's pressed affordance to actual occupancy. |
| `9092100780` | Adds the keyless web e2e for the management surface (trigger affordance, create through the drawer, row verbs, edit, delete, exit) and refreshes the sidebar/settings goldens; the preset e2e lists catch up with `schedule_update` and the pre-existing supervision/harness-source context drift. |
| `45ef6adb20`, `f377a72329`, `6a9428a914`, `dbe2704fcc`, `d2ca7718de` | Gives the client one breakpoint scale: `dsh-client-ui-layout` classifies its frame width into `phone`/`compact`/`regular` and publishes it as `data-viewport`, overlaying an expanded phone sidebar over the (inert) center column with a scrim confined to the strip the drawer leaves exposed. `dsh-client-ui-conversation`, `dsh-client-ui-settings-general`, `dsh-client-ui-theme`, `dsh-client-locale`, `dsh-client-ui-permission-presets`, and `dsh-client-ui-scheduler` consume that attribute instead of declaring private media queries — a stacking composer row, a wrapping stats line, a full-bleed settings sheet with a horizontal nav, and a schedule table that becomes one card per row. `dsh-client-web` ships the authentication documents' own `document.css`/`auth.css` and stops re-deriving a colour scheme the ui-theme index tap already resolved; the supervision chip now takes its mode names from the Host's own options. Composition, capabilities, and bundle rows are unchanged. |
| `a79c3c26f9` | Localizes the client's built-in product labels with host-customized names passing through untouched: `dsh-client-ui-conversation` renders the permission presets (仅可查看／工作区内修改／完全权限) and supervision modes (随时监督／无人值守, with descriptions and the aria prefixes) through the `conversation` namespace, `dsh-client-ui-permission-presets` ports the resolver and the `/permission` popup's `permission.access` keys, and `dsh-session-log-export` names its header action Session 日志; the full-access confirmations say 完全权限 in Chinese. On the phone form the composer's send/stop pair takes its own seat and a narrow card stacks the model row under the buttons with the send control spanning both lines (container queries; `.card` now carries the container because a row cannot match a query against itself), and the turn-end meta line wraps between its dot segments instead of overflowing. Composition, capabilities, and bundle rows are unchanged. |
| `7374d6090a` | Phone portrait takes the sidebar off the frame entirely in `dsh-client-ui-layout`: the `phone` form holds no sidebar grid track in either state (collapsed keeps the subtree mounted but inert and out of the a11y tree; expanded keeps the overlay drawer), so the center column owns the full width, and a frame-owned floating 36x36 entry button in the reserved leading corner (`layout` namespace `sidebar.open`, shared `IconPanelLeftOutline16` via a new peer dependency on `dsh-client-ui-primitives`) is the only portrait affordance — `dsh-client-ui-conversation`'s header cedes a matching 56px notch on the phone form. The drawer/scrim drop from z 26/25 to 18/17 so `shell.overlay` sheets opened from the drawer cover it, opening a center view exits the drawer, and Escape closes it via a capture-phase listener that peels one layer when both drawers are up. Landscape (`compact`) keeps its rail. Composition, capabilities, and bundle rows are unchanged. |
| `d9f7dcfb6e` | Adds the resource governor and its board: `dsh-governor` (`ctx.governor`) meters correlated shell/terminal spawns (one `/proc` walk per tick for CPU/RSS/PSS/disk, `ss -tinp` TCP attribution while tree members own sockets), enforces a global memory budget in tiers (cgroup-v2 parent+leaves when cgroupfs is writable; `prlimit --as=` prefixes plus a sustained-overage watchdog otherwise; observe-only as the floor), and arbitrates shared-pool session quotas with admission control, durable `quota_overrides` rows, log-only `governor/quota` audit events, and first-come resume replay that notifies the agent on clamps. The subprocess seam gains `SubprocessCorrelation`/`SubprocessLimits` on spawn specs and emits `subprocess/spawned`/`exited` (+terminal twins); `dsh-bash-local` forwards them, `dsh-tool-bash` stamps every bash call, resolves governor limits, and merges breach facts (`governor: {killed, peakBytes, limitBytes}`) into result meta; `dsh-terminal-bash` stamps its PTY sessions. The `resource-quota` model tool negotiates only its own session (one approval under the `ask` policy). The `governor` Typert Remote namespace (`overview`/`sessionSamples`/`sessionQuotaGet`/`breaches`/`configGet`/`sessionQuotaAdjust`/`reload`) mounts through `dsh-api-remotes` and `host.describe` publishes the tier; the new `dsh-client-ui-governor` board (sidebar footer trigger + `center.view`) polls it at the sampling cadence. `dsh-user-approval` exposes `effectivePolicy` for the raise gate. |
| `50729cc8c1` | Delivery follow-up for the governor: the `resource-quota` tool moves out of the service's global registration into a separately loadable `dsh-governor/tool` Consumer entry (package `exports` subpath pointing at the raw tsc `lib/types` emit; dependency-light so profiles' flat `node_modules` resolve it), mounted by the shipped `standard` preset's `tool-governor` row — compositions without the service keep an empty global tool layer. `subprocess-local` no longer blocks service start on the `prlimit` probe (constructor fire-and-forget; the probe result still fronts rate-limited spawns), which un-blocked tool-bash's deferred apply and restored the bash tool in first-assembled model requests; metering now emits the `exited` twin even for spawns that fail before acquiring a pid so paired events always close. The governor service exposes `admitExplicit`/`budgetLimitBytes`/`liveRssBytes` seams for the tool instead of approval internals. |
| `efdbe1151a` | Adds the Resource-governance settings page to `dsh-client-ui-governor`: a `settings.section` registration (id `governor`, ordered after Plugins) edits the one enforced global quota — the memory budget, `auto` or a custom GiB figure — through the client settings scope (`ctx.settingsScope.bind({ namespace: 'governor' })`, writes riding `settings.mutate` under the `harniverse.administer` gate; read-only copy without the capability), while the effective budget beside the form comes from the governor `configGet` Remote (host-resolved truth, reread immediately and 600 ms after an apply for the hot re-application). The host side gains no new write path — the service's `installSettingsSection` hook already re-resolves and re-applies on change. `formatBytes` moves to a shared `bytes.ts` module; browser-package dependencies gain `@deepseek-ai/dsh-client-ui-settings`; web settings/preset/plugin-config goldens refresh for the new nav entry. |
| `2770aacbc9` | Fixes the page above against real hosts: the api-proxy wire allowlist (`WEB_SETTINGS_NAMESPACES`) never listed `governor`, so the wire `settings.describe` omitted the registered section and every write answered `settings-not-exposed` — the page rendered its unavailable arm permanently. Exposure stays a decision made in the api-proxy; a wire-level regression pins describe plus a `settings.mutate` round-trip. |
| `3075dedff4` | Makes the governor honor committed budget changes: `installSettingsSection`'s `setSource` thunk is now kept (previously snapshotted at boot, so writes committed but never re-applied), and `applyGlobalLimit` takes a monotonic epoch that abandons superseded applies before any effect — the boot-time init apply and settings-attach apply raced, with the stale `auto` resolution settling last and clobbering the fresh budget. Gate-based regressions cover both interleavings; `src/index.ts` stays per-file 100%. |
| `ea8a578f79`, `68b625464b` | Gives custom models a declaration surface for what only `settings.yaml` could state: the Models page's per-model disclosure gains image input (`input` on pi-ai rows, `inputModalities` on DeepSeek rows), a reasoning declaration (offered levels with per-level wire spellings, at least one beyond `off`), the per-model default effort (`defaultReasoningEffort`, including `default` = explicitly send no effort, which stops the route default), and on openai-completions routes the thinking dispatch format with a `chat_template_kwargs` editor under the un-withheld `chat-template` format (literals or `thinking.enabled`/`thinking.effort` variables, `omitWhenOff`). Server side: resolution carries `configuredDefaultEffort` per model, `chat-template` leaves `WithheldThinkingFormat`, and kwargs are refused under any other format. Every downstream gate (`read_image`, image preflights, effort picker, model-switch defaulting) reads the resolved metadata unchanged. |
| `54097d990d` | Delivers the approved design's composer-side effort affordance: `dsh-client-ui-conversation` declares the named `conversation.input.effort` session seat directly right of the model seat (the plan/model pattern — `conversation.input.right` list entries render left of the model seat, so a sibling seat is the honest composition), and `dsh-client-ui-model-selection` registers `EffortButton` into it over the same per-session directory and `selectModel` verb as the model seat: advertised levels with the effective effort preselected, hidden while the current model declares no reasoning, catalog load/retry and empty postures staying on the model seat. The effective-effort/label/rows derivation moves to shared `effort.ts` helpers both components render from. |
| `13d119d125` | Retires the standalone effort button delivered above at the owner's direction: the model button's two-level menu already shows and switches the effective effort, so the second button duplicated an affordance for composer width. `conversation.input.effort` is withdrawn from ui-conversation (contract, children table, render site; catalog regenerated) and ui-model-selection returns to two entries (EffortButton, its spec, and the `effortButton.aria` keys deleted). The model menu's Effort pane, shared `effort.ts` derivation, and settings-page declarations are untouched. |
| `60d68484ee` | Overhauls session search end to end at the owner's direction. CJK recall: FTS5 `unicode61` gains bigram folding on both the indexing and query sides (`ngramFtsText`), so two-character sub-words match inside CJK/kana/hangul runs and whitespace-separated terms AND as independent quoted phrases (`quoteFtsData`); snippet presentation strips the inserted joins; `SESSION_QUERY_SQLITE_SCHEMA_VERSION` bumps to 10 so derived indexes rebuild. Tool merge: `session_event_search` is withdrawn and `session_search` absorbs the one-session scope — exactly one `session_ids` entry routes to the full event listing (caller's own session allowed, stopping before the active step, per-target authorization preserved) while absent or multiple ids keep per-session best-match reading; the shared prompt text and descriptions are rewritten as decision-path guidance that names compacted-turn searchability and the compaction-history recovery path. Extraction: `compaction/summary` events project their summary blocks into shared search documents (`dsh-session-query` gains the `dsh-compaction` type-only dependency). Performance: live-session fingerprints become the O(1) (event count, last seq, surface replace generation) identity, and an unchanged fingerprint skips cloning, folding, and document rebuilding before every search — the sha256-over-entire-log fingerprint and its per-search full observation cost are gone. |
| `8ed5459ecb` | Fixes session-search snippet fidelity and the cost of ranking in `dsh-session-query-sqlite`. The bigram folding that gives CJK/kana/hangul sub-word recall was written into the one indexed column that `highlight()` read back for presentation, and deleting the inserted joins is not an inverse of an overlapping-bigram expansion, so every interior CJK character reached the model doubled (`中文字符` shown as `中文文字字符`) and author spaces were eaten (`你好 世界` shown as `你好世界`). Document rows now store their source prose beside the folded text (`raw_text UNINDEXED`, NULL when folding changed nothing, mirroring the existing `raw_title` companion) and snippets project that copy; `codepoint_length` measures the prose so the relevance tie-break compares documents rather than encodings. Ranking now counts indexed term instances from each table's `fts5vocab(..., 'instance')` companion instead of rebuilding and re-scanning every candidate document with `highlight()`, keeping the counts source-comparable across the persistent and TEMP tables while measuring 516 ms → 43 ms and 690 ms → 91 ms on a 1200-document all-CJK corpus; a TEMP FTS5 tokenizer oracle supplies the exact index terms so `unicode61` case folding and diacritic removal are never reimplemented, and the count join is a LEFT JOIN so an uncounted document ranks last rather than vanishing. Query normalization stops baking in the index encoding (folding moves to the two MATCH-building sites) and the excerpt window is anchored by literal folded-term search in the prose. `SESSION_QUERY_SQLITE_SCHEMA_VERSION` bumps to 11 so derived indexes rebuild. |
<!-- compat-convention-start -->
<!-- New ledger rows land here. Every row after this marker declares its compatibility
     stance: `Compat: <claim>.` (or `Compat: none.` when no durable or public contract
     is touched) plus, for every non-none claim, `Verify: <command>.` naming the check
     that re-verifies it. `verify-ledger-compat` enforces the tail; the rows above this
     marker are the frozen pre-convention history and are exempt. -->
| `921cc3f8cf` | Adds the wave-3 first-batch contracts: the `dsh-image-offload-policy` package with the `image/offload` session event and per-image user-turn aging, the `dsh-session-import` package with the `import/record` archival marker, the MCP resource identity/visibility/refresh contract in `dsh-mcp-client` with the `mcp-resource` capability member kind, the `dsh-control-channel` frame/backpressure/lifecycle contract for PTC and SSH, and the `dsh-execution-descriptor` immutable execution-world contract refusing the Host-local `cordis` preset. Compat: v0 additive (new `image/offload` and `import/record` session events; `mcp-resource` member-kind widening). Verify: pnpm run verify-session-contract-digest && pnpm run verify-persistence-catalog && pnpm run verify-ledger-compat. |
| `921cc3f8cf` | Adds the v0 session-contract digest gate and the ledger compat convention: `gen-session-contract-digest` classifies structural versus additive drift against the committed `docs/session-contract-digest.json` baseline, and `verify-ledger-compat` enforces the `Compat:`/`Verify:` tail on every ledger row after the convention marker; both leaves join `doc-sync`. Compat: none (process gate; no session-serialized type, schema, plugin API face, or wire contract changes). |
| `be85dd0988` | Wave-3 W02 retirement: removes the never-wired `packages/e2b/` family (`dsh-e2b`, `dsh-fs-e2b`, `dsh-subprocess-e2b`), the inherited official `packages/schedule/schedule` (`dsh-schedule`) with the `examples/web-schedule` overlay and the manual `e2b-e2e` workflow, and the unused `dsh-schedule` dependency in `apps/cli`; rewrites `schedule-after` web e2e onto the downstream `dsh-scheduler` vocabulary; replaces the subsystems page with the downstream scheduler family page; `schedule/change` leaves the known-event vocabulary with its sole producer deleted. Compat: owner-approved v0 baseline removal (the never-produced-outside-the-deleted-package `schedule/change` event; deliberate digest re-baseline under the W02 retirement decision, pre-release no-compatibility stance). Verify: pnpm run verify-session-contract-digest && pnpm run verify-persistence-catalog && pnpm run verify-translation-pairing && pnpm run verify-ledger-compat. |
| `9f955939d6` | Wave-3 W03 lazy native loading: new `@deepseek-ai/dsh-lazy-require` (caller-relative cached CJS loader), with the eager `sharp`, `node-pty`, and `koffi` native imports wrapped for first-use loading in `dsh-attachment-local`, `dsh-subprocess-local` (plus its Windows inspector koffi), and `dsh-sandbox-windows-acl`; image-free and terminal-free startup loads no native binding (pinned by lazy-native specs on all three packages) and the PTY factory becomes an injectable seam. Compat: none (load-timing only; no session-serialized type, schema, plugin API face, or wire contract change). Verify: pnpm exec vitest run --config vitest.config.ts packages/util/lazy-require packages/attachment/attachment-local/tests/lazy-native.spec.ts packages/subprocess/subprocess-local/tests/lazy-native.spec.ts packages/sandbox/sandbox-windows-acl/tests/lazy-native.spec.ts. |
| `b61f1fec0a` | Wave-3 W04 coordinated reloads: new `@deepseek-ai/dsh-hmr-coordination` boot package — one exclusive queue with nesting and post-disposal rejection, chokidar exact-path `watchConfig` with consecutive-write merging, `hmr-coordination/config-update-failed` broadcast, and disposal drain — and `dsh-app-boot` `watchUserPatches` plus the `apps/cli` profile boot now register their user patch layers through `ctx.hmrCoordination` instead of the vendored stat-poll `registerConfig`; module replacement and Include refresh stay vendored. Compat: none (boot-lifecycle reload coordination; no session-serialized type, schema, plugin API face, or wire contract change). Verify: pnpm exec vitest run --config vitest.config.ts packages/boot/hmr-coordination packages/boot/app-boot.
| `2c3a0fe2e0` | Wave-3 W05 remote-mock: new `@deepseek-ai/dsh-remote-mock` test-support package — a `RemoteMockApiClient` extending the real `AbstractApiClient` wire surface with `RpcMethodMap`-keyed programmable unary dispatch, hand-pumped mux/host downlinks recording their `since` cursors, a `RemoteMockRpcError` → `internal` mapping, a bypass `ClientAuthentication` double, and `mountRemoteConnection` booting the real connection plugin over a new `connectionCarrier` override seam in `dsh-client-connection`. Compat: none (test-support carrier double; the `connectionCarrier` optional service read changes no shipped composition). Verify: pnpm exec vitest run --config vitest.config.ts packages/test-support/remote-mock. |
| `b7440acadd` | Wave-3 W06 dual-protocol DeepSeek: `dsh-llm-deepseek` splits into `common/` helpers plus `protocols/{chat-completions,messages}` behind one dispatching `DeepSeekAdapter`; `protocol` defaults to `messages` on the official `api.deepseek.com/anthropic` root and keeps `chat-completions` behind a custom base URL until set explicitly, Messages roots are validated HTTP(S) without credentials/query/fragment, both protocols refuse redirects, Messages authenticates with `x-api-key` + `anthropic-version: 2023-06-01`; Messages wire serializes content blocks with in-history system updates for capable models, strict tool pairing, malformed-history tool input as `{}`, image handles before file/base64 sources, and native thinking-signature replay (`deepseek-messages` v1 envelope, silent cross-model/foreign/degraded drops with one warn diagnostic); the Files API client speaks both flavors (chat OpenAI-shaped, Messages Anthropic-shaped with synthesized expiry and `files-api-2025-04-14` beta) under the protocol-scoped `deepSeekFileScope` upload index; the default catalog becomes `deepseek-flash` (V41, text+image, in-history) and `deepseek-v4-pro`, `imageDetail` retires in favor of `imagePixelBudget: number \| "low"` (512×512 preset), and reasoning gains a `low` effort on both wires; wire attempts report `api: 'messages'` / `'chat-completions'`. Compat: none (wire protocol addition behind an explicit config field; `llm/wire-attempt.api` is an open string, no session-serialized type change; the previous default catalog ids and `imageDetail` were never consumed outside this package's own tests). Verify: pnpm exec vitest run --config vitest.config.ts packages/llm/llm-deepseek/tests. |
| `bc5d6d5ff8` | Wave-3 W11 explicit deliverables: new `@deepseek-ai/dsh-tool-present` plugin — the model-facing `present(files: [{path, description?}])` tool validating existing regular files on the session filesystem within a `maxFiles` bound (default 8) and appending the log-only `deliverables/presented` event `{turn, callId, files}` to the owning agent's session only on successful results — plus the `turnBoundary` session projection owned by `dsh-agent-loop` (`turnBoundaryProjectionDefinition`, `stateVersion` 1, registered under the optional `sessionProjections` injection) that supplies the open-turn fact the tool reads mid-turn; `dsh-client-ui-deliverables` gains the presented fold (structural wire gate, latest-per-path before the closing seq) rendered as a second wrapping lane over the same loopback-gated `openFile` opener, and the `chatFileMentions` resolver widens to the produced+presented union; presets standard/code/cordis mount `tool-present` after `tool-web` (minimal stays without), `apps/cli` depends on the package, and the tool/persistence/cordis catalogs plus the session-contract digest and known-event types are regenerated (the W04 `hmrCoordination` SERVICE_PAGE/EVENT_SCOPE_PAGE mappings are healed in the same regeneration). Compat: additive session event only (`deliverables/presented` is log-only, merged by declaration, unknown to older replay which ignores it; no SQLite schema, plugin public API face, or wire contract change beyond that new event). Verify: pnpm exec vitest run --config vitest.config.ts packages/deliverables/tool-present/tests packages/core/agent-loop/tests packages/client/ui-deliverables/tests. |
| `f2100d11a7`, `1233242f45` | W07 MCP resources and instructions: scoped providers expose bounded resource and URI-template discovery/read tools, attributed instructions, and pre-dispatch member permissions. Provider-owned configuration capture pins MCP clients to each Profile generation; settings edits affect later assemblies. The base bundle mounts the resource registry, which contributes no tools without scoped providers. Compat: additive resource-registration and capability-generation APIs; existing v0 event payloads and SQLite schema are unchanged. Verify: NODE_OPTIONS=--max-old-space-size=1536 ./node_modules/.bin/vitest run packages/mcp/mcp-client/tests packages/mcp/mcp-resources/tests packages/mcp/mcp-user-config/tests packages/capability/capabilities/tests packages/preset/agent-presets/tests/mount.spec.ts --maxWorkers=1 --no-file-parallelism. |
| `2db5b0cfd0`, `1233242f45` | W12 durable image offload: the base bundle mounts Session message projections and request-time settlement; both DeepSeek protocols commit pressure omissions before serialization, including Files fallback. Per-occurrence decisions survive replay, SQLite restore, forks, compaction and pruning while originals remain available to authorized rereads. Compat: additive runtime request-projection APIs reuse the existing image/offload event without changing the v0 envelope or stored payload. Verify: NODE_OPTIONS=--max-old-space-size=1536 ./node_modules/.bin/vitest run packages/compaction/image-offload-policy/tests packages/compaction/compaction-image-offload/tests packages/core/session/tests/window-restore.spec.ts packages/core/session/tests/derived-cache.spec.ts packages/llm/llm/tests/service.spec.ts --maxWorkers=1 --no-file-parallelism. |
| `4056ecc1b1`, `1233242f45` | W17 archival import: strict official v1/v2/v3 decoding, native display mapping, exact source retention and rollback, authenticated workspace-bound HTTP intake, and plugin-owned Agent admission exclusion. Cold reads remain available; live creation, resume and execution forks reject imported history. Compat: additive authenticated POST /api/session/import and Agent admission APIs; imported logs reuse the existing v0 import/record marker and native display events, with no format or SQLite schema change. Verify: NODE_OPTIONS=--max-old-space-size=1536 ./node_modules/.bin/vitest run packages/session/session-import/tests packages/core/agent-loop/tests/resume.spec.ts packages/client/connection/tests/session-import-route.host.spec.ts --maxWorkers=1 --no-file-parallelism. |
| `040a3a674e`, `a824503467`, `ae03e0d54a` | Third-batch corrections preserve captured MCP generations, narrow image event carriers and prepared adapter inputs, require validated foreign creation timestamps, and preserve BOM handling during archival import. Boundary tests cover resource refresh/disposal, image projection races, import rollback and authenticated intake. Compat: no additional session payload, SQLite schema or HTTP contract change beyond W07/W12/W17 above. Verify: pnpm run verify-session-contract-digest && pnpm run verify-ledger-compat. |
| `99026c7370` | Preflight master-CI repairs: the packed-install e2e closes `WORKSPACE_CLOSURE` over the `packages/util/lazy-require` fixture the lazy-native loading mounts, and the rescope gate allows the execution-world contract's `cordis` preset id (product data, not a package specifier). Compat: none (test-fixture and gate fixes; no session-serialized type, schema, plugin API face, or wire contract change). Verify: pnpm exec vitest run --config vitest.e2e.config.ts packages/sandbox/sandbox-local/tests/packed-install.e2e.ts && ./node_modules/.bin/tsx scripts/rescope-vendor.ts. |
| `6ede621565` | Wave-3 fourth batch W08/W09: `dsh-control-channel` gains the multiplexed bounded-request transport shared by child processes; `dsh-code-runtime-ptc` replaces the retired `dsh-code-runtime-worker-thread` through a platform-correct fresh child (`nodeExecutable`/`bootstrapPath` spawn plan, pkg respawn-self mode), with dependent swaps in the bundles, tools presentation, spill-policy, the jsonrpc-demo packaged bin, and Python SDK packaging, plus regenerated single-exe smoke snapshots. Compat: owner-approved v0 baseline removal (the worker-thread Provider package retires; the `ctx.codeRuntime` Definition is unchanged and every shipped consumer swaps in the same change, pre-release no-compatibility stance). Verify: NODE_OPTIONS=--max-old-space-size=1536 ./node_modules/.bin/vitest run packages/subprocess/control-channel packages/subprocess/subprocess-local packages/code-runtime --maxWorkers=1 --no-file-parallelism. |
| `7cd693646b` | Wave-3 fourth batch W10: the POSIX SSH execution world (`dsh-ssh` verified helper, bounded control-channel RPC, lease heartbeats, machine-owned MCP/Skill/Hook inventory, immutable `WorldDescription`) with `dsh-fs-ssh`/`dsh-subprocess-ssh`/`dsh-sandbox-ssh` providers, isolated local sshd acceptance coverage, and asynchronous provider preparation seams: shell executor `start()` and sandbox `confine()` widen to additive union returns, bash/pwsh background jobs keep cancellation across asynchronous startup, and terminal resize rides the subprocess contract. Compat: v0 additive (new packages; the `start()`/`confine()` union widenings accept every existing synchronous provider and caller unchanged). Verify: NODE_OPTIONS=--max-old-space-size=1536 ./node_modules/.bin/vitest run packages/ssh packages/shell packages/sandbox/sandbox-local packages/terminal/terminal-bash --maxWorkers=1 --no-file-parallelism. |
| `77ccf38a09`, `2223a73309`, `c296435ffb` | Fourth-batch CI corrections: the ssh family reaches the per-file coverage bar through behavioral suites over an in-memory helper harness (wire, processes, connection, consumer, providers, invariants) with bounded `v8 ignore` fences; the sandbox provider resolves the bwrap/sandbox-exec runner programs to absolute executables before building confined argv so empty-environment PTC children spawn verbatim; the ssh, `subprocess-local/output`, and execution-descriptor tsconfig path entries lost in the reference port are restored so subpath imports resolve on the source plane; the POSIX-only ssh family joins the Windows-excluded suites per the `sandbox-local` precedent, and machine-owned hook tests spawn through the current node executable. Compat: none (the confined argv stays structurally identical — the runner `argv[0]` resolves to an absolute executable when found, which only makes the documented spawn-verbatim contract hold under empty child environments; no session-serialized type, schema, plugin API face, or wire contract change). Verify: NODE_OPTIONS=--max-old-space-size=1536 ./node_modules/.bin/vitest run packages/ssh packages/sandbox/sandbox-local --maxWorkers=1 --no-file-parallelism. |
<!-- compat-convention-end -->
| `0c325ae577960a88e0a55ad082dee816f224aee3` | W09 PTC naming/bootstrap and W16 desktop delivery: the renamed `dsh-ptc-runtime` Definition and `dsh-ptc-runtime-node`/Python Providers expose `PtcRuntime`/`ctx.ptcRuntime`, preserve durable `code` preset ids, `run_code`, event vocabulary and the Session schema, and support source, built, pkg and Electron bootstrap modes; the Desktop shell/Host adds authenticated exact-key enrollment, dedicated Profile/home ownership, native directory picking, lifecycle and shutdown policy, bundled Playwright/Chromium and pnpm payload validation, blue-H native branding, native packaging, local artifact updates and the packaging CI driver. Compat: v0 breaking for the pre-release renamed PTC service/package faces; durable Session schema, stored event contracts and code preset/tool ids remain unchanged. Verify: 147 desktop Vitest tests and 24 Node packaging tests passed; full build, typecheck, lint and hygiene passed; final branded Linux x64 AppImage and unpacked local qualification passed with inventory `0acb0a809a1433abc933c47862604401b2c911cb68548654fd4204c86db7518f`; Windows/macOS CI qualification remains pending. |
<!-- composition-changes-end -->

## Wave 3 third-batch composition

| Capability | Definition and provider | Consumers and shipped composition |
|---|---|---|
| MCP resources and instructions (W07) | `dsh-mcp-resources` owns scoped resource registration; `dsh-mcp-client` provides bounded resource/template discovery, reads, and attributed instructions. `dsh-mcp-user-config` captures private settings per Profile generation through `dsh-capabilities`. | `dsh-base` mounts the resource registry; its three shared tools exist only in scopes containing providers. MCP-enabled Profiles enforce inherited member allowlists before network reads, while existing generations retain their captured settings. |
| Durable image offload (W12) | `dsh-image-offload-policy` defines per-occurrence age and durable `image/offload` records; `dsh-compaction-image-offload` supplies Session message projections and `llm/project-request` settlement. | `dsh-base` enables offload. Both DeepSeek protocols settle pressure omissions before serialization, including Files-to-inline fallback; compaction and pruning preserve canonical stubs. Attachment bytes remain available to authorized rereads. |
| Foreign-session archival import (W17) | `dsh-session-import` parses official v1/v2/v3, maps display history into native v0, retains original bytes beside persistence, and registers archival admission policy on `ctx.agents`. | `dsh-base` mounts the service; authenticated `POST /api/session/import` requires `harniverse.operate`, bounds uploaded bytes, and resolves the destination through the Workspace registry. Imported history can be read and searched but cannot become a live Agent or fork for execution. |

Initial implementation commits: W07 `f2100d11a7`, W12 `2db5b0cfd0`, W17 `4056ecc1b1`. The generic admission and request-projection APIs keep import and image policy in their owning plugins. Generated catalogs, bilingual documents, and focused regression fixtures are supporting artifacts.

## Wave 3 fourth-batch composition

| Capability | Definition and provider | Consumers and shipped composition |
|---|---|---|
| Bounded control-channel transport (W08) | `dsh-control-channel` adds the multiplexed stdio transport over the first-batch frame contract: interleaved bounded requests, per-request cancellation, ordered release under backpressure, and loss that invalidates every outstanding request. | Consumed by the fresh-process PTC runtime and the SSH helper; no direct model-facing surface. |
| Fresh-process PTC runtime (W09) | `dsh-ptc-runtime` defines `PtcRuntime` / `ctx.ptcRuntime`; `dsh-ptc-runtime-node` spawns a platform-correct child through the `nodeExecutable`/`bootstrapPath` plan, including pkg and Electron self-execution, and `dsh-subprocess-local` supplies the collected-output reader and spawned-child entry. `dsh-ptc-runtime-python` remains opt-in. | Headless and Web bundles, spill-policy tests, Python SDK packaging and Desktop use the fresh-process Node Provider over the control-channel transport. Active copy uses PTC; durable `code` preset ids and `run_code` remain stable. |
| Captured POSIX SSH execution world (W10) | `dsh-ssh` verifies the helper digest, leases it with heartbeats, carries bounded RPC over the shared control-channel transport, and publishes the immutable `WorldDescription` (machine-owned MCP/Skill/Hook inventory, captured Profile restrictions, `cordis` refusal); `dsh-fs-ssh`, `dsh-subprocess-ssh`, and `dsh-sandbox-ssh` provide the remote seams over one shared connection. | Opt-in remote deployments only; no shipped Web composition mounts the family, and the deployment-owned bootstrap pairing is the same seam the PTC runtime consumes. |

Initial implementation commits: W08+W09 `6ede621565`, W10 `7cd693646b`, with the preflight CI repair in `99026c7370`. Generated catalogs, bilingual subsystem documents, the type-equivalence manifest, and the isolated sshd acceptance fixture are supporting artifacts.

## Wave 3 fifth-batch composition

| Capability | Definition and provider | Consumers and shipped composition |
|---|---|---|
| Full-access ambient environment inheritance | `dsh-subprocess` specs gain `ambientEnv?: 'full' | 'scrubbed'` ('scrubbed' default preserves the scrub), `dsh-subprocess-local` switches `childEnv` on it, and `dsh-bash-local`/`dsh-pwsh-local` stamp `'full'` only under `danger-full-access`. | Full-access shells and the W13 user terminals match the user's interactive environment up to the documented `ENV_OVERRIDES`; confined spawns and the model PTY stay byte-identical to before. |
| Authenticated user terminal panel (W13) | `dsh-api-terminal-controller` (`ctx.terminalController`: per-Session idempotent create over `spawnTerminal` with `ambientEnv: 'full'`, the `term: 'xterm-256color'` terminal type the PTY seam publishes as `TERM` — the seam's `'dumb'` default leaves `clear`, colour, and full-screen programs inert — and login+interactive shell startup, headless-xterm screen recovery, single-controller attachment fencing, unattended reclamation) rides the `terminal/*` gateway Remote verbs beside the `events.terminal`/`events.hold` SSE streams, with `dsh-deque` follower queues, `dsh-typert-protocol` `RemoteError` passthrough, and the `dsh-host-apiproxy` carrier codes; `dsh-client-ui-terminal` mounts the xterm.js workbench section. | Enabled by `dsh-web-app`; terminal traffic is a system-user surface — never sandbox-confined, never model-visible, never in the Session log. |
| Host browser carrier panel (W14) | `dsh-api-browser-controller` (`ctx.browserController`: per-Session identity-idempotent create, privilege-resolved Chromium sandbox (`sandbox: 'auto'`), executable probing that reports what it looked for when the host has no browser, navigation policy at the CDP boundary — http/https only, embedded-credential and non-network scheme refusal —, JPEG screencast frames over the `events.browser` stream, input dispatch, navigation history, unattended reclamation) rides the `browser/*` gateway Remote verbs; `dsh-client-ui-browser` renders the frames and forwards pointer/wheel/keyboard input as a Workspace-workbench section; conversation markdown links open in the section through the `openWorkbenchSection(section, request?)` seam, or in the reader's own browser when the `ui-conversation.linkDestination` preference says `'device'`. | Enabled by `dsh-web-app`; a human-only carrier — no tools, no session events, nothing model-visible, and page egress originates from the host process rather than the user's device. |

Initial implementation commits: ambient environment `661a41b184`, W13 host `bad4b0bb1c`, W13/W14 client panels `45a691f127`. The terminal panel presentation and empty-state defects were fixed in `20a9ef76f9`, and W14 was redefined from the client sandboxed iframe to the host CDP browser process in `a51a4a2801` (see the host-browser-panel-egress Agent Note; the earlier iframe design is superseded). `d58173ce68` fixed the two shipped defaults that made both panels unusable in a root container — the Chromium sandbox selection and the PTY terminal type — see the panel-browser-and-terminal-unusable-defaults Agent Note. `9e2c36ec61` made a browserless host diagnosable and gave the reader the `ui-conversation.linkDestination` choice between the host's browser and their own — see the host-without-a-browser-and-the-readers-own Agent Note. Generated catalogs, bilingual subsystem documents, the type-equivalence manifest, the terminal-controller host Agent Note, and the refreshed lifecycle goldens are supporting artifacts.

## Architecture Refactor Ledger

<!-- refactor-ledger-start -->
Status: **refactor implemented**.

| Refactor | Problem | Target state | Status |
|---|---|---|---|
| Auth management App | Universal launcher parsed provider-specific token grammar and imported `dsh-authentication-local` outside the plugin tree. | The minimal `dsh-auth-app` bundle/profile owns parsing and execution; `dsh auth` is only a profile alias. | Complete |
| Tool final-result extension | `dsh-tools` owned spill policy and hardcoded a separately composed Consumer after definition finalization. | `dsh-tools` exposes the generic asynchronous `tools/finalize-result` waterfall before immutable commit. | Complete |
| Result artifact Consumer | Retention and `artifact_read` could be composed independently even though successful retention promised the tool existed. | `dsh-tool-result-artifacts` owns retention, marker, failure semantics, and retrieval tool in one plugin. | Complete |
| Spill reference contract | `SpillRef.retrievalHint` let a storage Provider own model-facing wording. | `SpillRef` carries only opaque locator and exact bytes; Consumers own presentation. | Complete |
| Notification role projection | Generated capability tables showed no Consumer even though the Definition package folds the coordinator Consumer. | Generated graphs explicitly label the bundled coordinator role for notification and session telemetry. | Complete |

The base bundle, four Web agent presets, standalone auth profile, package manifests, and generated catalogs use these final package boundaries.
<!-- refactor-ledger-end -->

## Maintenance Rules

- Update this file in the same change that adds, removes, renames, combines, or splits a first-party plugin package.
- Record a capability as the complete Service Definition / Service Provider / Consumer relationship; never present constituent packages as independent capabilities unless they actually are.
- Record bundle, profile, preset, and scope changes separately from package inventory changes.
- Mark generated catalogs, tests, and documentation as supporting projections rather than runtime plugin packages.
- Keep the baseline immutable. Upstream synchronization must add a new dated baseline section rather than silently replacing the imported tree.
- Every ledger row after the compat-convention marker declares a `Compat:` stance — the compatibility claim, or `none` when the change touches no session-serialized type, SQLite schema, plugin public API face, or wire contract — and a non-`none` row also names its `Verify:` command. A digest change alone never proves compatibility; the claim and the command are the review surface.
