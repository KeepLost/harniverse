/** Host loader entry for the Profile-composition browser implementation exported from `./client`. */

/** Host plugin body — capability management lives in its authorized Host package. */
export function apply(): void {}
