/**
 * Client-namespace projection of the scheduler domain: a pure re-export of
 * the package's types outlet, the `dsh-goal/client` shape.
 * @module @deepseek-ai/dsh-scheduler/client
 */

export type * from './types.ts'
